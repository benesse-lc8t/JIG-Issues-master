# TBBS-JIG（経営Issue管理システム）— ツールの目的と設計思想

> このドキュメントは Claude（および管理者）が**毎セッション最初に必ず読む**前提のものです。
> リポジトリの閲覧権限は管理者のみ。社員には共有しません。記述は率直に書いています。
> 設計や運用で迷ったときは、まずこのドキュメントの原則に立ち返って判断してください。

---

## 1. 何のためのツールか（一行）

**結果を報告させる会議**から、**進捗を見ながら方向をアジャストする会議**へ転換するための、Issue/Mission の常時可視化ダッシュボード。

このツールは**二重の目的**を持つ：

- **(A) 経営幹部が Issue から逃げない構造をつくる**（常時可視化で「向き合うべきテーマから離れられない」状態を維持）。
- **(B) 経営戦略組（詩雅・育菱＝事務局）の業務負荷を下げる**。会社全体の膨大な Issue/Mission を管理しやすくし、事務局の仕事を「転記・代行という**作業**」から「**催促・方向づけ**」へ移す。

## 1.5 路線転換（2026-05-31）：本人週次記入＋ログ蓄積へ

> 本節は §2・§3・§5・§6 の一部記述を**上書き（supersede）**する。矛盾する旧記述（特に「本ツールへの本人入力はゼロ」）は本節を優先すること。

### 何を変えたか
- これまで「進捗の実体は Confluence／本人は本ツールに入力しない」としていたが、**本人（戰略負責人・実務担当）が週1で Mission の進度を本ツールに直接記入する**方式へ転換する。
- 記入は**上書きせず、追記専用ログ（append-only）として蓄積**する。過去の進度が時系列で残り、Mission の歩みを追える。
- これにより本ツールは「ハブ／俯瞰図」から、**進捗パルスの一次記録面**へ格上げされる。

### なぜ変えるか（目的）
1. **回収可能なログをためる**：散文が散る Confluence では横断分析できない。スプレッドシートに `編號 / 日時 / 担当 / 進度` の**行**で溜めれば、停滞検知・要約・横断分析ができる。
2. **AI に読み込ませる**：溜めたログを**AI のコンテキストとして読み込ませ**、進度の要約・停滞の検知・会議論点の抽出を AI に担わせる前提。構造化された行ログがこの用途の燃料になる。
3. **事務局（詩雅・育菱）の負荷を下げる（目的B）**：本人が直接書くことで事務局の転記作業が消え、仕事が「催促・方向づけ」へ集約される。

### 「逃げ場のなさ」はどう保つか（核は不変）
- 自己申告へ一部戻るが、ログ化により**「書いていない週＝空白」が時系列で露呈する**。単一セルの「更新日が古い」より、**記入リズムの途切れ**のほうが雄弁。「前回記入から○日」を赤表示するなど空白を可視化すれば、逃げ場のなさはむしろ強まる。

### 負荷を上げない歯止め（フィルタ②の死守）
- 記入欄は**一行・短文固定**。重い情報・成果物は Confluence リンクへ逃がし、**報告フォーム化させない**（§4 と矛盾させない最重要点）。
- 「週1の一言」を Confluence から本ツールへ**移設**するだけ＝本人の総負荷は増やさない（むしろ Confluence への遷移が消えて軽くなる）。

### 構造方針（既存を壊さず“足すだけ”）
- 既存 `Mission一覽` は不変。H列（Mission進度）には**ログ最新行＝現況をミラー**し、既存の一覧UI・色変化はそのまま使う。
- **新タブ `Mission進度ログ`（追記専用）** を足す：`編號 / 日時 / 担當 / 進度`。保存時に1行 append。
- ダッシュボードはこのタブも gviz で読み、編號でグループ化。最新＝現況、モーダル/hover でタイムライン全文表示。
- Apps Script は既存の書き込み経路に **appendRow を1本足す**（上書きから追記へ）。

### 著者・権限
- 本人が書く＝**記入者の識別が要る**。当面は「記入者を名前で選択」で割り切り、**本物の認証は次フェーズ**。静的サイト＋クライアント露出の共有 `WRITE_TOKEN` という現状は本人運用には弱い、と自覚しておく。

### Confluence の再定義
- 進捗パルスが本ツールへ移るため、Confluence は**成果物・資料・深い履歴の置き場**へ再定義（§6 参照）。現況の二重管理はしない（一次ソースは本ツール）。

> **実装状況（2026-05-31 v0.24.0）**：**コード実装済み**。`Mission進度ログ` タブ（`setup-sheet.gs`）、`doPost` の `進度`＋`擔當` 追記＋現況ミラー、ダッシュボードの記入者プルダウン＋「今週の進度」＋タイムライン表示まで実装。**管理者の有効化作業が残る**：①`setupJIG` を 1 回実行してログタブを作成、②Apps Script を同一 URL で再公開（§9）。記入者識別は当面「名前プルダウン（シートの擔當/戰略負責人から自動）」、本物の認証は次フェーズ。誤記修正はログタブを直接編集（UI からの過去エントリ編集は今後）。

## 2. ツールが果たす役割

- **Issue → Task の階層構造**を管理する（Issueに紐づく具体タスクも同じ場で見える）。
- 各 Issue / Mission は **Confluence の対応ページへのURL**を持つ。**成果物・資料・深い詳細**の実体は Confluence 側。本ツールは入口（ハブ）であり、かつ**進捗パルス（週次の一行）の一次記録面**でもある（§1.5 路線転換）。
- 進捗は「数値や長文の入力」ではなく、**軽い手段**で表現する（例：ステータスの一段階／Confluence最終更新日／簡易チェック）。**毎回アウトプットを求めるツールではない。**
- 必要なタイミングが来たときだけ、アウトプットを引き出す合図を出す。

## 3. 設計の核（社員には公言しない／管理者の本音）

**Issue を常に・全員の目に晒し続けることで、各自が自分の向き合うべきテーマから自然に離れられない状態を作る。**

- 「報告という作業」ではなく「**Issue への常時の意識**」を醸成する。
- 別の言い方をすれば、**逃げ場のない構造を、強制ではなく可視化で実現する**。
- これは正面切って社員には言わない。だが、本ツールのすべての意思決定はこの軸で行う。
- 対外的（公開ページ・社員説明）には「進捗共有・方向アジャスト・報告負荷低減」のトーンで語る。

## 4. 設計判断のフィルター（迷ったらこれで判断する）

新機能・変更を入れるかどうかは、以下を**全て**満たすか確認する：

1. **常時可視化を強めるか？** Issue/Task が忘れられにくくなる方向か。
2. **報告負荷を上げないか？** 社員に新たな入力作業を課していないか。重ければ却下。（※2026-05-31 路線転換〜：本人の**週次一行記入は許容**。ただし Confluence からの**移設**であり、**一行・短文固定**が条件。報告フォーム化・長文入力は引き続き却下。§1.5 参照。）
3. **Confluence への動線を阻害していないか？** 詳細はあくまで Confluence。本ツールに詳細を抱えさせない。
4. **管理者の判断・アジャストを助けるか？** 会議で「どこを掘るか」が見えやすくなるか。

これらに反する機能（例：本ツール内での長文進捗入力、複雑なワークフロー、報告フォーム化）は基本的に却下。

## 5. データモデル（2026-05-31 v0.10.2 で更新）

スプレッドシートは **2 タブ運用**。Task は別シートに切り出して編集負荷を下げる。

### タブ1：`Issue主檔`（Issue マスタ）
既存列（変更なし）：
`區分 / 軸 / 編號 / Issue / Issue短縮 / 處 / 組 / 戰略負責人 / 管理頁面 / 確認會議 / 會議時間`

追加列：
| Sheet列名 | 値 | 記入者 | 用途 |
|---|---|---|---|
| **Confluence URL** | 組ページURL + `#編號` アンカー | 事務局（数式で自動化可） | 入口リンク |
| **狀態** | `未開始` / `進行中` / `完成` の3段階 | **事務局（会議時に更新）** | 進度の段階表示 |
| **事務局備註** | 自由テキスト（観察メモ） | **事務局（随時更新）** | 例：「○○確認中」「6/5 レビュー予定」「3週間停滞」 |
| **更新日** | 日付 | 事務局（更新時に手動 or 自動） | 古いものを色変化で目立たせる |

> `區分` `軸` 列はシートに残すが、**ダッシュボードでは v0.5.0 から非表示**（情報過多のため）。

### タブ2：`Mission一覽`（旧 `Task一覽`／v0.10.2 確定）

全部門の Mission を 1 枚に集約する。事務局が会議中・終了時にここで編集する。

**実際のシート列構造（A〜J、10列）：**

| 列 | Sheet列名 | 値 | 必須 | 備考 |
|---|---|---|---|---|
| A | **編號** | `戰略-1-T1` など | ✓ | 親編號 + `-T番号` 推奨 |
| B | **Mission** | Mission 内容 | ✓ | （旧列名 `Task` / `Issue` と同義） |
| C | **親編號** | `戰略-1` 等 | ✓ | `Issue主檔` の編號と一致 |
| D | **管理頁面** | 管理ページURL | ─ | 空なら親を継承 |
| E | **戰略負責人** | 担当者名 | ─ | 親と違ってもよい。空なら親を継承 |
| F | **擔當** | 実務担当者 | ─ | |
| G | **狀態** | `未開始` / `進行中` / `完成` | ─ | Issue と同じ3段階 |
| H | **Mission進度** | 自由テキスト（観察メモ） | ─ | 旧列名 `備註` / `事務局備註`。ダッシュボードからインライン編集可能 |
| I | **更新日** | 日付 | ─ | ダッシュボードからインライン編集可能 |
| J | **Confluence URL** | URL | ─ | 空なら親を継承 |

> **列名の注意**：`事務局備註` は `Issue主檔` シートの列名。`Mission一覽` の対応列は **`Mission進度`**（H列）。混同しないこと。

**継承される項目**（Mission シートに書く必要なし）：
`處 / 組 / 確認會議 / 會議時間`（ダッシュボード側で親 Issue から自動補完）。

> H列（Mission進度）は路線転換後、**`Mission進度ログ` の最新行＝現況をミラー**する位置づけ（既存の一覧UI・色変化をそのまま使うため）。直接の上書き入力は誤記修正用の escape hatch として残す。

### タブ3：`Mission進度ログ`（追記専用／2026-05-31 路線転換・**未実装**）

本人の週次進度を**1記入＝1行**で溜める追記専用ログ。回収・AI コンテキスト供給・停滞検知の燃料（§1.5）。

| 列 | Sheet列名 | 値 | 備考 |
|---|---|---|---|
| A | **編號** | `戰略-1-T1` 等 | どの Mission の記入か（`Mission一覽` の編號と一致） |
| B | **日時** | `yyyy-MM-dd HH:mm` | append 時にサーバーが自動付与 |
| C | **擔當** | 記入者名 | 当面は名前選択。認証は次フェーズ |
| D | **進度** | 一行・短文 | 上書きしない。重い詳細は Confluence リンクで |

> 保存時に Apps Script が本タブへ appendRow し、同時に `Mission一覽` H列（現況ミラー）と更新日を更新する。**実装済み（v0.24.0〜）**。

### タブ4：`人員`（名簿の一次ソース／2026-05-31 v0.25.0）

記入者チップの名前・色（處ベース）の出どころ。管理者がここに名簿を貼る。

| 列 | Sheet列名 | 値 | 備考 |
|---|---|---|---|
| A | **姓名** | 育菱 等 | 記入者チップの名前。一次ソース |
| B | **處** | 經營戰略 等 | チップの**色の単位**（一覧の部門色 dept-c-* と一致） |
| C | **組** | 1.經營戰略 等 | 補助（将来の絞り込み用・任意） |
| D | **顯示順** | 数値 | 並び順（任意。空なら處→姓名順） |

> `setupJIG` で自動作成。ダッシュボードは gviz で読み（`window.personMap`）、記入者ドロップダウンの名簿・色・並び順を生成。**人員タブが無ければ Mission の擔當/戰略負責人 から名前収集・色なしにフォールバック**（後方互換）。處が空の人はグレー。

### 「詰まり」は別シグナルで拾う
ステータスは3段階に絞り、「詰まり」はステータスに置かない。代わりに：
- **事務局備考**に状況テキストで表現（例：「営業組確認待ち（5/15から停滞）」）
- **最終更新日**の経過で自動色変化（>7日黄、>14日赤）

これにより停滞が可視化される。（※2026-05-31 路線転換後は本人の週次記入も併用するが、ログ化で**「空白の週」が露呈する**ため逃げ場のなさは保たれる。§1.5 参照。）

### 役割分担（2026-05-31 路線転換で更新）

| 担当 | やること | 入力先 | 頻度 |
|---|---|---|---|
| **本人**（戰略負責人・實務擔當） | Mission の進度を**一行で記入（ログに追記）**。重い詳細・成果物は Confluence リンクで | **スプレッドシート（本ツール）** | 週1（推奨） |
| **事務局**（管理者＋経営戦略組＝詩雅・育菱） | ステータス3段階の更新・**催促と方向づけ**（転記作業はしない） | スプレッドシート | 確認会議の都度／随時 |

> **旧記述「本ツールへの本人入力はゼロ」は撤回（2026-05-31）。** 本人が本ツールに週次で一行を直接記入する。これは「**一行・短文固定＋ Confluence からの移設**」により**総負荷を上げない**形で行い、事務局の転記作業を消して**事務局の仕事を催促・方向づけに集約**する（目的B）。フィルタ②と核（逃げ場のなさ）の両立は、ログ化による**「空白の週の露呈」**で担保する。詳細は §1.5。

## 6. Confluence ページ設計（2026-05-30 確定）

> **2026-05-31 路線転換による再定義**：週次の**進捗パルス（現況の一行）は本ツールへ移設**したため、Confluence は**成果物・資料・深い履歴の置き場**が主役割になる。以下の「現況欄を週1で上書き」運用は、本ツールへ一次ソースが移った分だけ軽くなる（現況の二重管理はしない）。雛形・履歴ログ・Tasks の考え方自体は引き続き有効。

### 1組1ページ方式
- 報告負荷・閲覧負荷を最小化するため、**1組（部門）1ページ**で完結
- 1人が1ページを週1で更新
- 雛形ページは**事務局が用意**。記入者は記入に集中

### ページ構造（A型：State型＋履歴ログ別欄）

```
# [組名] Issue管理ページ — 担当: ○○

📌 今週の動き（更新: YYYY-MM-DD）   ← トップ。週1で上書き
- 動いた: 共-1, L-2-T1
- 詰まり: 共-7（API待ち）
- 来週の山: 共-3

───────────────────────────────
Issue 共-1  交叉銷售·客單價提升  [進行中]
■ 現況（5/29）: テスト準備中。来週金曜実施予定。   ← 1〜2行・上書き型
▸ 履歴ログ（折りたたみ Expand マクロ）              ← 節目だけ追記
▸ Tasks（Confluence Task List）                    ← チェックリスト
───────────────────────────────
Issue 共-2 …
```

### 「最新と履歴」の方式：State型（A型）を採用
- **現況欄は最新で上書き**（古い文は消えてよい。Confluenceの編集履歴で版自動保存）
- **履歴ログは節目だけ手動追記**（フェーズ転換、重大判断、停滞解除）。毎週書く必要なし
- 効果：「いまどうか」専用の欄があり、日付が古いと**動いてないが露呈**。State の利点と Event Log の軽さを両立

### ダッシュボードからのリンク
- Issue/Task 行の Confluence URL は **組ページURL + `#編號`アンカー**
- スプレッドシートで `=組ベースURL & "#" & 編號` で自動生成可能（事務局が組ベースURLだけ管理）

## 7. アーキテクチャ（現状）

- 静的サイト（`index.html` 単一ファイル）を Netlify で公開。main ブランチ更新で自動デプロイ。
- データの正は **Google スプレッドシート**（gviz CSV）。`index.html` 起動時に取得して全タブを描画。
- 管理者向けの「資料來源」設定タブは **右上の「⚙ 管理者專用」ボタン**からのみ開ける（一般ユーザーのタブ行からは見えない）。
- 静的サイトの性質上、UI隠蔽は「人目から隠す」レベル。URLやソースから到達は可能。**厳密なアクセス制御は不可**である点は受け入れている。

## 8. 公開時の説明トーン（参考）

社員向けに本ツールの目的を語るとき：

- 「報告作業を減らし、Issue への意識を高めるためのもの」
- 「アウトプットは Confluence に集約し、ここはそれらの俯瞰図」
- 「会議は結果報告ではなく、進捗共有と方向アジャストの場へ」

「逃げ場をなくす」「常時監視」など、管理側の本音を示唆する語は使わない。

### 8.1 UI 言語ポリシー（恒久・厳守）

- **ツール内に表示されるテキストは、すべて繁體中文（zh-Hant）で実装する。** 利用者は台湾スタッフのため。
- 管理者（小沼氏）への会話・依頼は日本語で行われるが、それは**作業指示の言語**であって、**成果物（UI 文言）は繁中**。日本語の UI 文言を新規に入れない。
- 例外：コード内コメント・`CLAUDE.md`/`CHANGELOG.md` 等のドキュメント・コンソールログは日本語可（利用者の目に触れないため）。
- 新機能・修正で文言を足す/変えるときは、トースト・プレースホルダ・ボタン・ラベル・エラーメッセージまで含めて繁中で書くこと。

## 9. 開発運用メモ

- 作業ブランチ：`claude/keen-einstein-9NcDI`（指定があれば変更）
- main への push は明示指示があれば fast-forward で実施
- 変更時は本ファイルの該当節を更新し、判断の整合を保つ
- 重要な方針変更があれば、本ファイル末尾の「変更履歴」に追記
- バージョンは `index.html` 冒頭の `APP_VERSION` と右上バッジで管理。変更ごとに上げて CHANGELOG.md に追記する運用。

### Apps Script デプロイの注意点（重要）

- Apps Script の Web App は **デプロイごとに異なる URL** を持つ。「新しいデプロイ」を作ると新 URL が発行される。
- `index.html` の `SHEET_WRITE_URL` は**そのデプロイの URL**に合わせること。古いデプロイ URL のままだと、新しいコードは実行されない（旧バージョンが動き続ける）。
- コード変更後は：①スクリプトを保存 → ②既存デプロイを「バージョンを管理」→「新しいバージョン」として更新（同じ URL を維持）する。**新しいデプロイ**を作ると URL が変わるので `SHEET_WRITE_URL` の更新も必要。
- `doGet()` でレスポンス JSON に `WRITE_FIELDS` / `sheetName` / `headers` / `remarkColIndex` を含めているので、デプロイ後に URL を直接叩いて診断できる。
- `getActiveSpreadsheet()` は**スタンドアロン Web App では使えない**（null を返す）。`SpreadsheetApp.openById(SHEET_ID)` を使うこと。`setup-sheet.gs` の先頭の `SHEET_ID` 定数を正しいスプレッドシート ID に設定しておく。
- `SpreadsheetApp.getUi().alert()` はスタンドアロン実行コンテキストでは**ハングする**（例外をスローしない）。`console.log` に置き換えること。

## 10. 次セッションへの引継ぎ（2026-05-31 時点）

新しいセッションを始めたら、まず本ファイルと `CHANGELOG.md` を読むこと。以下が現状サマリ。

### 現在の状態
- **最新版：v0.22.1**（`index.html` 冒頭 `APP_VERSION` と右上バッジで確認）
- 作業ブランチ：`claude/stoic-fermat-McakE`（旧 `claude/keen-einstein-9NcDI`）。**毎回コミット＆ `main` にも push 済み**（明示指示で main へ反映する運用が定着）。
- 直近の大きな作業テーマは **Mission進度タブ（Tab C）の編集 UX 刷新** と **Confluence リンク表示の整理**。詳細は CHANGELOG v0.16〜v0.22。

### この期間で入った主な機能（UI/UX）
- **統合編集モーダル**：Mission進度セルのダブルクリック（または狀態/🔗セルのクリック）で、狀態・Mission進度・Confluence URL を 1 つのモーダルで編集。Ctrl+Enter 保存／Esc キャンセル。ヘッダーに ISSUE / MISSION を 2 段表示。
- **楽観的更新＋伸びるバー**：保存は約6秒かかるため、入力値を即時反映しバックグラウンド保存。セルが左→右に満ち色が変化（約5秒）。
- **Confluence リンク**：1 セルに複数 URL を `名前|URL`（改行区切り）で保持（名前なしは URL のみ＝後方互換）。`parseConfEntries()`/`serializeConfEntries()`/`confLabel()`/`confEntryLabel()` が中核。一覧はアイコンのみ＋件数バッジ、複数はクリックで「Confluence 連結」モーダル、単一は直接リンク。ヘッダーは favicon（`conf-th-icon`）。
- **表示モーダル（読み取り専用）**：他タブ（Issue&Mission/D/E）の Mission進度セルクリックで、編集モーダルを `readOnly` 再利用で表示。✎編輯ボタンで Tab C の編集モーダルへ遷移。
- **ダイジェストテキストウィンドウ**：Mission進度セルのホバーで画面中央に本文を大表示（約350ms、操作不可、`pointer-events:none`）。
- **列幅リサイズ**：localStorage 永続（キー `jig:colw:panel-X`）＋リセットボタン。

### 完了済み（旧 TODO①）
1. **Apps Script の再デプロイは完了**（2026-05-31）。`scripts/setup-sheet.gs` 側の「Confluence URL 編集時も `更新日` を自動更新」「応答 JSON の `更新日` に時刻 `HH:mm` を含める」が**デプロイ済みで、サーバーが実際にこの応答を返す**。
   - 該当コード：Confluence URL 編集時の `touchedContent`→更新日自動セット（setup-sheet.gs L179-200）、応答の `更新日` を `yyyy-MM-dd HH:mm` 形式で返却（同 L206）。
   - `index.html` の `ensureEditTimestamp()`（L1803-1811）は**防御的フォールバックとして残置**。引数に時刻が既にあればそのまま返す実装なので、再デプロイ済みサーバーの応答（時刻入り）は素通りし、二重処理も値の改変も起きない＝無害。撤去は任意。

### 残りの TODO（任意）
2. **スプレッドシートの `更新日` 列の書式**を「日時」にすると、シート上でも時刻まで見える（任意）。
3. Confluence リンクの**手書きラベル**は実装済み（v0.19.0）。さらに別カラム管理にしたい等の要望が出たら検討。

### よく使う識別子
- `SHEET_ID`（setup-sheet.gs 先頭）／`WRITE_TOKEN = 'JIG-WRITE-TBBS-2026'`／`SHEET_WRITE_URL`（index.html）。
- Apps Script `WRITE_FIELDS`：`{狀態, 備註→Mission進度, 更新日, Confluence URL}`。

## 変更履歴

- 2026-05-31 (v0.27〜v0.28): 新增モーダルを**繁中統一**＋**追加/編集 兼用**化。擔當/戰略負責人を**名前チップピッカー**（複数選択・×削除・處ベース色／戰略負責人は `STRATEGIC_LEADS` の4名限定）に。Issue の「組」は「處」連動プルダウン。新增ボタンは**進行バー**。Tab A 各行の **✎ で既存 Issue/Mission を編集**（名称・人物・狀態・處/組・Confluence、**編號は不変**）。一覧の人物セルを**處カラーのチップ表示**（`personChipsHtml`）。Apps Script に `updateMission`/`updateIssue` 追加（数式列・入力規則を尊重してスキップ）。UI 言語ポリシー §8.1 を明文化。

- 2026-05-31 (v0.27.0): **UI 言語ポリシーを §8.1 に明文化（UI 文言はすべて繁體中文・恒久厳守）**。新增モーダルを繁中化。擔當／戰略負責人を名前チップピッカー（處ベース色）に、Issue の「組」を「處」連動プルダウンに、新增ボタンに処理中スピナーを追加。
- 2026-05-31 (v0.26.x): ダッシュボードからの Issue／Mission 追加を安定化。append は数式列を壊さず（アンカー先書き＋自動算出列スキップ）、データ入力規則違反は flush を try 内で発火させ捕捉してスキップ（warnings 通知）。gviz ヘッダー誤検出対策に `headers=1` ＋ `rowsToData` のヘッダー探索。`doGet` に `codeVersion`（再公開確認用）。
- 2026-05-31 (v0.26.0): **ダッシュボードから Issue／Mission 追加**を実装（目的B：事務局がシート直編集していた行追加を画面内の最小フォームに集約）。Tab A の「＋ 新增」→ トグルで Issue／Mission を追加。Mission は親 Issue 配下に `親編號-M{n}` を**自動採番**、Issue は編號**手動＋重複チェック**。フォームは最小項目固定、詳細・成果物は Confluence へ（フィルタ②③遵守）。Apps Script `doPost` に `addMission`/`addIssue` の append アクションを追加（`_appendByHeaders` でヘッダ名マッピング書込み＝列順非依存）。**同一 URL での再デプロイが必要**。認証は引き続き次フェーズ（現状は共有トークン）。
- 2026-05-31 (v0.25.x): 人員マスタ（`人員` タブ）導入で記入者チップを處ベースの色に。チップ小型化・`總管理` をピンク化・記入者の既定値を「王詩雅」に。編集モーダル表示中の背景スクロール裏抜けを修正。
- 2026-05-31: **路線転換（ドクトリン更新）**。①ツールの目的を**二重目的**に明文化：(A) 経営幹部が Issue から逃げない構造、(B) 経営戦略組（詩雅・育菱＝事務局）の負荷低減（事務局の仕事を「作業」から「催促・方向づけ」へ）。②**本人が週1で Mission 進度を本ツールに直接記入し、追記専用ログとして蓄積**する方式へ転換（旧「本人入力ゼロ」を撤回）。狙いは**回収可能な行ログ**と**AI へのコンテキスト供給**、および事務局の転記消滅。③逃げ場のなさは「空白の週の露呈」で担保、負荷は「一行・短文固定＋Confluence からの移設」で上げない。④Confluence を成果物・資料の置き場へ再定義。⑤新タブ `Mission進度ログ`（編號/日時/擔當/進度）を設計確定（**コード未実装**）。記入者識別は当面名前選択、認証は次フェーズ。§1.5 を新設し §2/§3/§5/§6 の該当記述を更新。
- 2026-05-31: **Apps Script 再デプロイ完了を反映**（ドキュメント更新のみ）。旧 §10 TODO①（「Confluence URL 編集時の `更新日` 自動更新」「応答 JSON に時刻 `HH:mm` を含める」）はデプロイ済みで完了。`index.html` の `ensureEditTimestamp()` は無害な防御的フォールバックとして残置（撤去は任意）。作業ブランチ名を `claude/stoic-fermat-McakE` に更新。
- 2026-05-31 (v0.16〜v0.22): **Tab C 編集 UX 刷新と Confluence 表示整理**。統合編集モーダル（狀態/Mission進度/Confluence URL）、楽観的更新＋伸びるバー、複数 Confluence リンク対応（`名前|URL` 形式・件数バッジ・リンク一覧モーダル・favicon ヘッダー）、読み取り専用「表示モーダル」、ホバーの「ダイジェストテキストウィンドウ」、列幅リサイズ永続化を導入。詳細は CHANGELOG.md。（当時 Apps Script 再デプロイは未実施だったが、**同日中に再デプロイ完了**。上の履歴を参照。）
- 2026-05-29: 初版作成。ツール本来の目的（進捗報告会議への転換、Issue常時可視化、逃げ場のなさを可視化で実現）と設計判断のフィルターを明文化。
- 2026-05-30: データモデル確定（ステータス3段階／事務局備考／最終更新日／種別・親編號）。役割分担を明文化：**ステータス・事務局備考の入力者は事務局**、本人はConfluenceの現況更新のみ。Confluenceは**1組1ページ・A型（State型＋履歴ログ別欄）**で確定。雛形は事務局が用意。「詰まり」はステータスから外し、事務局備考と最終更新日経過で代替。
- 2026-05-30 (v0.5.0): Tab A から `區分` `軸` を撤去（情報過多）。**Task を別シート `Task一覽` に分離**（編集負荷低減）。親 Issue からの列継承で Task シートの記入項目を最小化。CLAUDE.md §2「Confluence が詳細の入口」は維持しつつ、Task 自体の運用ハブはダッシュボード側に置く（柔軟運用）。
- 2026-05-31 (v0.10.2): **タブ名変更**：`停滯警報` → `Mission進度`（赤 #EA4335、`Issue & Mission` タブの隣に配置）。**列名統一**：`Mission一覽` の備考列を `備註`/`事務局備註` から `Mission進度`（H列）に確定。`事務局備註` は `Issue主檔` 専用列名として明確化。`setup-sheet.gs` に `SHEET_ID` 定数・`_getSpreadsheet()` ヘルパー追加（スタンドアロン Web App 対応）。`setupJIG` を非破壊的に改修（既存シートはヘッダ名変更のみ、データ削除なし）。`diagnoseMissionSheet()` / `fixMissionHeaders()` デバッグ関数追加。Apps Script デプロイ URL 管理の注意点を §9 に明文化。
