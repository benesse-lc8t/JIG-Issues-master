# JIG リデザイン計画 — 3層カスケード／名前起点プラットフォーム化

> 起草：2026-06-01（壁打ちセッションの結論を文書化）。作業ブランチ `claude/cool-bell-vs8ov`。
> 本書は **実装の一次ソース**。CLAUDE.md の旧フィルター②③（報告フォーム化しない／一行固定）を、§1 の「正体の転換」で**意識的に上書き**する。
> 作り方は **現行 `index.html` を段階改修**（小沼氏決定 2026-06-01）。動くものを保ちながら段階リリースする。

---

## 1. 正体の転換（最重要・意識的な舵切り）

JIG はこれまで **「Issue→Mission の2層／進捗パルス（週次一行）を可視化するダッシュボード」** だった。
今回、**「Issue→Mission→Task の3層で全社業務を回すハブ／プラットフォーム」** へ格上げする。

- 旧ドクトリン（CLAUDE.md §4 フィルター②③＝報告フォーム化しない・一行固定・詳細は Confluence へ）は、本書 §1 で**部分的に上書き**する。
- 新しい歯止めは「**負担だから却下**」ではなく「**使う方が便利だから自然に開く**」。出社の比喩＝義務だが、仲間と情報がそこにあるから結局それが速い、を UX で実現する。
- ただし「軽さ」は捨てない。記入は**ストレスゼロ**（インライン＋モーダル両対応、どのセルも直せる、作業位置保持）。重い成果物は引き続き Confluence/SharePoint へ**リンクで逃がす**（連携＝賢いリンク集）。

---

## 2. 確定設計判断（握り済み）

| # | 項目 | 確定内容 |
|---|---|---|
| D1 | 階層 | **Issue → Mission → Task の3層**。DRI＝處長 → 組長 → 組員 |
| D2 | Issue 状態 | **7状態を付けない**。起動者は総経理（小沼氏）、幹部と握る。粗く `進行 / 結案` 程度 |
| D3 | Mission/Task 状態 | **7状態**：`構想段階 / 企画中 / レビュー前 / 実行中 / 終了 / 凍結 / 中止` |
| D4 | 終了系の扱い | `終了(結案) / 凍結 / 中止` は**既定で畳む**が**振り返れる**（アーカイブ閲覧） |
| D5 | 人の関係 | **DRI ＋ 複数コラボレーター**。層・部門跨ぎ可。基盤部門は本質的に協作 |
| D6 | 個人ビュー | **名前起点**。DRI が上段、協作が下段。Issue/Mission/Task 全層を一望 |
| D7 | 画面 | ①管理職（處長・組長）＋組員は**1画面完結** ②総経理・詩雅・育菱は**俯瞰＋管理ログ** |
| D8 | カスケード健全性 | Issue/Mission に**定義欄**を持たせ明文化。状況に応じ修正可。親定義を子編集時に常時可視 |
| D9 | 入力 | **モーダル＋インライン両対応**（新規・修正とも）。どのセルも修正可 |
| D10 | 連携⑦⑧ | **賢いリンク集**（リンクを貼りやすい）。Confluence/Canva/mindmap/ChatGPT/Claude/Gemini/NotebookLM、365データは SharePoint へ |
| D11 | ノーストレス | 自動リロード／リロード時も作業場所保持／モーダルに過去ログ表示 |
| D12 | UI 言語 | 表示テキストは**すべて繁體中文**（CLAUDE.md §8.1 厳守）。本書・コメントは日本語可 |

---

## 3. 新データモデル

既存タブを壊さず**足す**方針。Apps Script は append/update 経路を拡張する。

### 3.1 タブ構成（変更点）

| タブ | 状態 | 変更 |
|---|---|---|
| `Issue主檔` | 既存 | **`Issue定義` 列を追加**（D8）。状態は3段階のまま運用（7状態化しない＝D2） |
| `Mission一覽` | 既存 | **`Mission定義` 列を追加**。**状態を7状態へ拡張**（D3）。協作の持ち方を §3.3 へ |
| `Task一覽` | **新設** | 3層目の実体。下表 §3.2 |
| `Mission進度ログ` | 既存 | 流用。将来 `編號` に Task 編號も入れられるよう汎用化（任意） |
| `人員` | 既存 | 流用（DRI/協作の名前・處カラーの一次ソース） |
| `個人備註` `公告` | 既存 | 流用 |

### 3.2 `Task一覽`（新設・列案）

| 列 | 名称 | 必須 | 備考 |
|---|---|---|---|
| A | **編號** | ✓ | 親Mission編號 + `-K{n}`（例 `戰略-1-M2-K1`）自動採番 |
| B | **Task** | ✓ | 行動の内容 |
| C | **親編號** | ✓ | `Mission一覽` の編號と一致（カスケードの鎖） |
| D | **DRI** | ─ | 組員。空なら親Mission継承 |
| E | **協作** | ─ | 複数可（§3.3 の表現方法） |
| F | **狀態** | ─ | 7状態（D3） |
| G | **進度** | ─ | 一行・短文（重い詳細は Confluence/SharePoint リンク） |
| H | **更新日** | ─ | インライン編集可 |
| J | **連結** | ─ | リンク集（`名前|URL` を複数、§3.4） |

> 継承：`處 / 組 / 確認會議` 等は親 Mission→Issue から自動補完（既存の継承ロジックを3層へ延長）。

### 3.3 「DRI＋複数協作」の表現（D5）

- 当面は**列内マルチ値**（区切り文字 `,` または `、`）で複数協作を保持。既存の人物チップピッカー（`window._namePickSel`／複数選択）がそのまま流用できる。
- 別タブの関係マスタ（`関係`：編號／姓名／役割）への正規化は**次フェーズの選択肢**として残す（まずは列内マルチで軽く始める）。
- `人員` タブの處カラーでチップ色分け（既存 `personChipsHtml`）。

### 3.4 連結（リンク集）列の形式（D10）

- 既存 Confluence の `名前|URL` 複数形式を踏襲し、**サービス種別アイコン**を付ける（confluence/canva/mindmap/chatgpt/claude/gemini/notebooklm/sharepoint）。
- 行内「＋連結」で**貼るだけ**。URL から種別を自動推定（ドメイン判定）。

---

## 4. 2画面のワイヤー

### 4.1 メイン画面（管理職＋組員・1画面完結／D6 D7①）

```
┌───────────────────────────────────────────────────────────┐
│ JIG  📢 公告…                        [經營俯瞰 ▸][管理 ▸] │
├───────────────────────────────────────────────────────────┤
│ 你是誰？ [ 選擇你的名字 ▾ ]            （選択は保持＝D11） │
├───────────────────────────────────────────────────────────┤
│ ▍我負責（DRI）                            ← 上段・優先     │
│   Issue（處長のとき）→ Mission（組長）→ Task（組員）       │
│   を自分の役割の層を主に、親子をたどれる形で表示          │
│   ・狀態クリック変更／本週進度インライン記入／＋連結       │
│   ・親定義（Issue定義・Mission定義）を常時隣に表示（D8）  │
│                                                           │
│ ▍我協作（コラボ）                         ← 下段          │
│   他者がDRIで自分が協作の Issue/Mission/Task。把握中心、  │
│   記入も可。                                              │
│                                                           │
│ ▸ 已結案／凍結／中止（畳み・振り返り用＝D4）               │
└───────────────────────────────────────────────────────────┘
```

### 4.2 俯瞰・管理画面（総経理・詩雅・育菱／D7②）

- 全社の **Issue→Mission→Task ツリー**を俯瞰。カスケード健全性チェック（親なし・定義空・停滞>N日を炙り出し）。
- 既存の Tab D/E/F・管理者專用・公告編集・進度ログ閲覧をここへ統合。
- 管理ログ機能（記入の有無＝空白の週の可視化、進度ログのタイムライン）。

---

## 5. 段階リリース計画（壊さず進む）

| Phase | 内容 | 出る価値 | Apps Script |
|---|---|---|---|
| **P0** | 本書確定・CLAUDE.md §1.6 追記でドクトリン整合 | 設計の一次ソース確定 | なし |
| **P1** | データモデル拡張：`Task一覽` 新設、`Issue定義`/`Mission定義` 列追加、Mission 7状態化 | 3層目の器ができる | `setupJIG` 拡張・`addTask`/`updateTask`・7状態許容 |
| **P2** | 名前起点メイン画面：DRI 上段／協作 下段、3層ツリー表示、インライン記入、親定義の常時表示 | 社員が自分の全層を一望・記入 | 読取は gviz、書込は P1 の action |
| **P3** | 俯瞰・管理画面の統合：ツリー俯瞰＋カスケード健全性チェック＋既存管理機能の集約 | 経営の俯瞰と健全性確認 | なし（読取中心） |
| **P4** | 連結リンク集の作り込み（種別アイコン・貼るだけ・自動推定）、結案アーカイブ、状態の畳み | 業務ハブとしての利便 | 連結列の書込 |
| **P5**（任意） | 関係の正規化（`関係` タブ）、本人認証、進度ログの AI 要約・停滞検知 | 拡張・分析 | 追加 action |

各 Phase 末に動くものをコミット → ff で main 反映（CLAUDE.md §9）。Apps Script 変更時は**同一 URL で再公開**＋`setupJIG` 実行が必要（§9）。

---

## 6. オープン論点（実装中に詰める）

- ~~O1：協作の持ち方~~ → **確定：列内マルチ値で軽く開始**（区切り `,`/`、`、既存チップピッカー流用）。正規化は P5 の任意拡張。
- O2：7状態の色設計（既存3状態の色と整合させる）。【実装時に詰める】
- O3：カスケード健全性チェックの判定ルール（親なし／定義空／停滞日数しきい値）。【P3 で詰める】
- ~~O4：DRI 判定~~ → **確定：明示入力**（各層の DRI を名前で指定。空なら親継承）。
- O5：認証は引き続き次フェーズ（現状は名前選択＋共有トークン＝CLAUDE.md §7）。

---

## 変更履歴
- 2026-06-04：**IT 向けデモ方針を追記**（詳細は `../docs/worklog-2026-06-04.md`、CLAUDE.md §1.7）。IMT の**表示**粒度を **Issue&Mission に限定**（Task は既定非表示・トグル残置。Task の実体は将来 mm-app 側）。**D6/4.1 の「IMT で3層ツリー表示」は取り下げ**。クリック編集モーダル → **mm-app 遷移（当面ダミー別タブ）**、**クイック一覧ビューワー**追加。※ Apps Script の Task一覽・`addTask`/`updateTask` は将来 mm-app 連携用に保持（破棄しない）。
- 2026-06-01：初版。3層化・名前起点・プラットフォーム化の確定判断（D1〜D12）、新データモデル、2画面ワイヤー、段階計画（P0〜P5）を確定。
