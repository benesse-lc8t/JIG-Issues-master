# 作業ログ — 2026-06-04（IT 向け「理想形デモ」へ IMT を寄せる）

> 作業ブランチ `claude/eager-davinci-UHzsa`（本セッション指定。main への自動 ff はしない）。
> 背景思想の説明物：`docs/IMT-mm-vision.html`（繁中／日本語トグル・IT 提示用）。

## 0. 目的（このフェーズの狙い）
資訊事業處（IT）に、**現状で考えている「理想形」をできるだけ実物デモで見せる**。
- データは全部ダミー（`seedFullDummy`）。削除・作り替え自由。
- IMT は「俯瞰・確認」、編集・思考は mm-app（マインドマップ）という**分業**を、画面で体現する（vision.html の図1）。

## 1. 確定した方針（小沼氏・2026-06-04）
- **IMT の表示粒度＝Issue & Mission まで**。**Task は IMT に出さない**。
  - 実装は **既定で非表示（層級トグルは残置）**。将来 Task は mm-app 側が持つ前提。
- **クリック→編集モーダルは廃止方向**。代わりに Mission 行から **mm-app 画面へ遷移**。
  - 当面は **別タブでダミーの mm-app 画面（静的 HTML）** を開く。実物 mm-app は後日差し替え。
- **クイック一覧ビューワー**（さっと全体を見渡せる俯瞰ビュー）を追加する。

## 2. 着手順（ログ更新 → 順次改修）
1. **ログ更新**（本ファイル＋ CLAUDE.md §1.7 ＋ REDESIGN-PLAN 追記 ＋ mm リポジトリ SESSION_LOG 注記）。← 済（v0.52.0）
2. **Task 既定非表示**：`window._myLevelShow` 既定 `task:false`、`myLvTask` の `checked` 撤去。トグルは残置。← 済（v0.52.0）
3. **モーダル廃止＋mm-app ダミー遷移**：Issue/Mission 名クリックの「定義／編輯」モーダルを廃止し `_myOpenInMm()` で `mm-demo.html?kind&id&name` を別タブ表示。Mission 行の ＋T 撤去、進度セル単クリック→インライン編集に統一。`mm-demo.html`（心智圖風プレースホルダ）新規。← 済（v0.53.0）
4. **快速一覽（俯瞰ビューワー）**：全画面オーバーレイで 處→Issue→Mission をテキスト羅列（等幅・1行1項目・情報密度優先）。處ヘッダー＝部門色＋件数、Mission は `├/└` ツリー＋`編號/名稱/狀態/@DRI/進度抜粋`。篩選欄で即時絞り込み（空の處は自動で畳む）。Mission クリックで mm-app（ダミー）遷移。Esc／背景で閉じる。読取専用。← 済（v0.54.0）

### 実物 mm-app 差し替えメモ
- 遷移先は `_myOpenInMm()`（index.html）の URL のみ。実物 mm-app 完成後はここを差し替えれば連携が生きる（編號でディープリンク）。

## 3. 現状コードの要点（調査メモ）
- メイン画面は「我的（My Work）」1枚（`renderMyView`）。`window._myLevelShow={issue,mission,task}` で層級表示を制御（CSS `.my-table.hide-task` 等）。
  - 既定初期化：`index.html` の `window._myLevelShow = ... || {issue:true,mission:true,task:true}`（→ task:false へ）。
  - 復元：`_myRestoreView`／保存：`_mySaveView`（`imt:myView`）。トグル配線：`myLvIssue/myLvMission/myLvTask`。
- 編集 UI：`.cell-editable`（ホバー✎）＋編集モーダル。→ 段階3 で mm-app 遷移へ置換。
- ダミー投入：`seedFullDummy`。

## 4. 留意
- UI 文言は繁體中文（§8.1 厳守）。
- 本セッションは `claude/eager-davinci-UHzsa` にコミット／push（main への ff はしない）。
