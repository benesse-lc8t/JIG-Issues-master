# 作業ログ — 2026-06-01（リデザイン：3層カスケード／名前起点「My Work」）

> セッションの作業記録。作業ブランチ `claude/cool-bell-vs8ov`、毎コミット main へ ff 同期。
> 一次ソース設計：`REDESIGN-PLAN.md`。ドクトリン：`CLAUDE.md §1.6`。

## 0. 出発点
- v0.35.0（タイポgrafiトークン体系まで）。Issue→Mission の2層ダッシュボード。

## 1. 設計確定（P0）
- 壁打ちで方針確定 → `REDESIGN-PLAN.md` を新規作成・コミット。
- 確定判断：**Issue→Mission→Task の3層／全社業務ハブ化**、Issue は7状態なし、Mission/Task は**7状態**（構想中/策劃中/待審核/進行中/結案/凍結/中止）、**DRI＋複数協作**、**名前起点**の個人ビュー、定義欄でカスケード健全性、連携は賢いリンク集。
- 旧 `未開始→構想中`・`需確認→待審核`・`完成→結案` は**表示時に読み替え**。

## 2. Apps Script データモデル拡張（P1）`scripts/setup-sheet.gs`
- 新タブ **`Task一覽`**（編號/Task/親編號/DRI/協作/狀態/進度/更新日/連結）。
- `Issue主檔` に **`Issue定義`**、`Mission一覽` に **`Mission定義`** 列を非破壊追加。
- 状態を**7状態**へ（旧2状態は書込許容＝移行期互換、プルダウンは allowInvalid:true）。
- `doPost` に **`addTask`/`updateTask`**、`_handleAddTask`/`_nextTaskId`。`_handleUpdate` 許容欄に Task系＋`事務局備註`。
- `addMission`/`addIssue` も定義列を書込。`doGet` 診断に `taskSheetExists`/`statusValues`。
- **`CODE_VERSION = gs-2026-06-01-cascade1`**。
- 旧「`Task一覽`＝Mission別名フォールバック」は index.html 側で**廃止**。

## 3. 「My Work」画面（P2 → P2改）`index.html`
- **v0.36.0**：名前起点メインをカード型で新設（DRI上段/協作下段）。
- **v0.37.0**：小沼氏モックアップに合わせ **階層テーブル**へ作り替え（カード廃止）。
  - 列：NO./Issue/Mission/Task/Link/folder/DRI/狀態/狀態紀錄/協作/戰略組擔當/記錄日。
  - NO. は `#01 / #01-M01 / #01-M01-T01` の**表示簡略化**（編號は不変）。
  - 狀態クリック→7状態、狀態紀錄ダブルクリック記入、Task名ダブルクリック改名、＋M/＋T。
  - Issue定義/Mission定義 を編集モーダルに追加。
- **v0.37.1〜37.3**：フォント統一、3層地色、自分チップ光彩、名前セレクターは一旦プルダウン、親重なりセル白。
- **v0.38.0**：列ベース配色（NO./Issue＝Issue色、Mission列＝Mission色）、濃青ヘッダー白文字センター、ヘッダーに iPhone風トグル、チップ70%。
- **v0.39.0（⑧）**：DRI を**モーダル式チップピッカー**化（上部フィルタ＋各行 DRI/協作/戰略セル編集の両方）。ピッカーに onChange フック追加。
- **v0.40.0（9点）**：DRIチップ70%、メニュー横展開、Mission非表示でTask残す、トグル70%＋白/青、**Only my DRI** スライド追加、NO.省略なし、**列幅リサイズ＋Reset**、**My Work のみ英語化**（戰略組擔當→**Supporter**）、記錄日を漢字相対（今天/昨天/N天前）。
- **v0.40.1**：列ごと非表示、幅リサイズ修正（th実幅基準）、横スクロール廃止（width:100%）、Show all/closed もスライド＋Only my DRI と排他、選択後の氏名タグ70%。
- **v0.40.2**：列トグルをヘッダー→**ctrl-bar へ移設**（列を消してもトグルが残り復活可能に）。
- **v0.41.0**：**Issue/Mission/Task を1つの「Item」列に統合＋インデント階層**（列 12→10、横スクロール対策）。ヘッダーフォント縮小。

## 4. ダミーデータ投入ツール（`scripts/setup-sheet.gs`）
- AIC4 組織ページ（全51名）を基に **`seedJIGDummy()`**：実在 Issue 配下に Mission（組＝1）＋ Task（各人＝DRI）。Issue 無い部門（財務/總管理）は範例 Issue を自動作成。進度先頭に `〔範例〕`。
- **`clearJIGDummy()`** で範例行を一括削除。
- タイムアウト（6分上限）対策で **`_bulkAppend`**（列ごと一括 setValues＋flush 1回）に改修。

## 5. ⚠️ 管理者（小沼氏）に残る作業
1. Apps Script に `scripts/setup-sheet.gs` 最新を反映 → **`setupJIG` 実行**（Task一覽・定義列・7状態の入力規則）。
2. **同一 URL で再公開** → `doGet` の `codeVersion` が **`gs-2026-06-01-cascade1`** を確認。
3. （UI確認用）**`seedJIGDummy` 実行**でダミー投入。不要になれば `clearJIGDummy`。
- 未実施でも Issue/Mission の**表示・狀態/進度記入は可**。**Task の追加/編集・各セルからの担当割当・定義表示**は再公開後に有効。

## 6. 現在地・次の候補
- 最新：**v0.41.0**（main と作業ブランチ同期済み）。
- 未着手：P3 俯瞰/管理画面統合＋カスケード健全性チェック、P4 連結リンク集（種別アイコン）/folder=SharePoint 保存列/結案アーカイブ、P5 関係正規化・認証・AI 要約。
- 直近の調整候補：横スクロールが残る場合の列幅圧縮、Item 列インデントの視認性、Task 単クリック詳細モーダル、協作の複数選択保存タイミング。
