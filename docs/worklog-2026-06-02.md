# 作業ログ — 2026-06-02（IMT 仕上げ＋本格化リデザインの壁打ち）

> 作業ブランチ `claude/cool-bell-vs8ov`、毎コミット main へ ff 同期。
> 設計の一次ソース：`docs/redesign-notes.md`（未確定）／`docs/IMT-briefing-for-chat.md`（チャット用）。

## 0. このセッションの2本立て
1. **実装（確定・反映済み）**：旧タブ撤去〜メインページ仕上げ（v0.48.0 → v0.51.2）。
2. **設計の壁打ち（未確定）**：締切・削除/アーカイブ・データモデル再設計・共有パーマリンク・可視性制御。→ `docs/redesign-notes.md`。

---

## 1. 実装した変更（v0.48.0 → v0.51.2）
- **旧タブ撤去**：A=Issue&Mission / B=會議議程 / C=Mission進度 / D=詩雅 / E=育菱 / F=DASHBOARD を削除し、**メインを「我的（My Work）」1枚に集約**。`rowsToData` の必須列から `區分/軸` を除外し、レガシー列（區分/軸/處/組/確認會議/會議時間/管理頁面/Issue短縮）を**スプレッドシート側で削除可能**に。`事務局備註` は My Work が使用継続で残置。`show()` を null 安全化。
- **「我的」タブボタン撤去**（唯一ビューのため）。管理画面に「← 返回 我的」。
- **ブランド/余白**：タブ名 `IMT`。起動画面は「ロゴ静止＋一度だけ満ちるバー（3s）」→ さらに **オーロラ背景のみ**に。外周/ヘッダー余白を圧縮、カード角丸縮小。
- **色・タイポ（重要）**：人名＝**處カラーの小ドット＋黒文字**（中華圏の"赤文字で人名"タブー回避。塗りチップ廃止）。狀態＝**淡色バッジ＋枠線**（青系の行地色に溶けない）。自分＝**ドットだけ穏やかに発光**。I/M/T バッジ 70%。表内フォントの**ウェイト/サイズを IMT 列（10px/400）に統一**。行ホバーを**薄ピンク**。
- **My Work 機能**：①検索窓（虫眼鏡SVGのみ）②`僅我負責`→`僅我DRI`（DRI＝擔當/DRI のみ絞り込み）③名前ピッカーを**チーム別・等幅グリッド＋長体オートフィット**、検索アイコンSVG化④DRI 選択タグも黒文字＋ドット⑤インライン入力欄を**細枠＋フォーカス青光り**⑥DRI 欄と検索バーの高さ統一。
- **テーブル構造**：**ヘッダー完全固定**（パネルを縦flex化＝フィルタ行固定／表本体のみスクロール）。フィルタ行とヘッダー間に隙間。**進度列を可変化**して右あふれ解消（既定列幅も圧縮）。
- **UX**：リロード時に**前回の表示状態（トグル）を復元**（`顯示全部` の固定 ON を解除）。
- **ダミーデータ**：`seedFullDummy`（全 Issue×2M×2T＋定義）健全確認済み（Drive MCP で実検証）。

## 2. 追加ドキュメント
- `docs/data-dictionary.md`：シート列↔UI呼称↔削除可否の対照表。
- `docs/redesign-notes.md`：本格化リデザインの**決定ログ＋未決の問い**（締切/削除アーカイブ/データモデル2面/共有パーマリンク）。
- `docs/IMT-briefing-for-chat.md`：Claude.ai に貼る**自己完結ブリーフィング**（管理者限定の本音含む）。
- `docs/mockup-chips.html` / `docs/mockup-loading.html`：比較モック。

## 3. 設計の壁打ち到達点（すべて未確定・詳細は redesign-notes.md）
- **締切（目標期日）導入で確定**。核は「**助けであって鞭でない**」。「2つの圧（停滞＝過去／締切＝未来）」で逃げ場のなさを再定義。未決：意味（target1種）/層（Mission芯？）/入力者（DRI本人？）。
- **削除/アーカイブ**：正常終了＝**アーカイブ（履歴保持）**／削除＝**誤登録のみの例外（ソフト削除・管理画面に隠す・親子ブロック）**。
- **データモデル**：**現況（上書き・1行）＋イベントログ（追記・多行）の2面**。現況は I/M/T を**1枚に統合**、ログは別1枚。**編號は不変・版はログで管理**。
- **共有パーマリンク**：編號ベースのディープリンク（`#/m/<編號>`）で最新集約ビュー → 送付/Confluence貼付で**双方向**。①ディープリンク→②軽量ビュー→③Confluence API。
- **可視性制御（公開/非公開/チーム内）**：**現アーキテクチャでは"本当の"制御は不可**（全データがクライアントに来る・gviz公開・認証なし＝UI隠蔽どまり）。実現には **Google Workspace 認証＋Apps Script を行フィルタ付き読取APIに**＋シート非公開化が必要。原則：**privacy は"構想/私的ドラフト"に限定**し、確定 Issue/Mission は可視（逃げ場のなさを守る）。

## 4. 次の一手（並行トラック）
- **(a) ドクトリン壁打ち＠Claude.ai**：`docs/IMT-briefing-for-chat.md` を貼って思想を詰める。確定後ここへ戻る。
- **(b) マインドマップ連携**：別リポジトリ `benesse-lc8t/LC8T-mindmaptool-2026` と連携検討（カスケード＝木構造で相性良）。本セッションは jig-issues-master に固定のため読めない → **GitHub App に当該リポジトリ権限付与＋新セッション**が必要（または README/主要ファイルを貼る）。連携の鍵は「ノードに IMT の**編號**を持たせる」「source of truth の分担」「双方向リンク」。
- **(c) 実装の再開**：方針確定後、CLAUDE.md / REDESIGN-PLAN.md に反映 → **軽い順（並べ替え→締切の表示→アーカイブ→…）**で段階実装。

## 5. 現状サマリ
- **最新版 v0.51.2**（右上バッジ）。作業ブランチと main は同期。
- アーキテクチャ不変（静的 index.html ＋ Sheet gviz 読取 ＋ Apps Script doPost 書込）。Apps Script 再公開は本セッションの変更では不要（フロント中心）。
