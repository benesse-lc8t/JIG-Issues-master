# 變更履歷 / Changelog

> 版號は [Semantic Versioning](https://semver.org/lang/zh-TW/) を緩く採用。
> UI 右上の `v0.x.y` バッジで現在版が確認できる。デプロイ確認はこのバッジを基準に。

---

## v0.48.2 — 2026-06-02

**[クリーンアップ] 「我的」タブボタンを撤去**

メインビューが「我的（My Work）」のみになったため、唯一残っていたタブボタン（タブバー）を削除。`panel-My` は既定 `active` で常時表示。

- 管理者 Console（右上 ⚙ バッジ）を開いた後に「我的」へ戻れるよう、Admin パネル冒頭に **`← 返回 我的`** ボタンを追加（旧タブが担っていた復帰導線の代替）。
- Admin の説明文の「試算表欄位順序」を現状（レガシー列削除後）に合わせて簡素化し、対照は `docs/data-dictionary.md` を参照する旨に変更。

## v0.48.1 — 2026-06-02

**[ホットフィックス] 旧タブ撤去後にページが読み込めない不具合を修正**

v0.48.0 で `#panel-A` を削除したが、起動時の `parseInitialTable()` が組み込みテーブル `#panel-A tbody` を前提に `tbody.querySelectorAll(...)` を呼んでおり、`tbody` が null となって `Uncaught TypeError: Cannot read properties of null` で初期化が停止していた（ローディング画面のまま）。

- `parseInitialTable()` を null 安全化：組み込みテーブルが無ければ `[]` を返し、そのまま `loadFromSheet()` でクラウドデータを取得する（組み込みテーブルは元々クラウド読込前の暫定フォールバックに過ぎない）。
- 他の `#panel-A/B/C tbody` 参照・`searchA`・`dashPipeline` 等は、いずれも撤去済みの描画/フィルタ関数の内部にあり起動経路から外れているため影響なし。

## v0.48.0 — 2026-06-02

**[クリーンアップ] 旧タブ撤去 → メイン画面を「我的（My Work）」へ一本化**

3層カスケード移行に伴い、役目を終えた旧タブ群を撤去した。

- **撤去したタブ／パネル**：`A=Issue & Mission` / `B=會議議程` / `C=Mission進度` / `D=詩雅` / `E=育菱` / `F=DASHBOARD`。タブ行・パネル DOM・管理者專用ページの詩雅/育菱ランチャー（▶）を削除。残るメインビューは **`我的（My Work）`** のみ（＋管理者專用 Console）。
- **レガシー列のコード依存を解消**：`rowsToData` の必須列から **`區分`/`軸` を除外**。これにより `區分/軸/處/組/確認會議/會議時間/管理頁面/Issue短縮` への稼働コード依存が消え、**スプレッドシート側で安全に削除可能**になった（手順は `docs/data-dictionary.md`）。`事務局備註` は My Work が Issue 進度メモに使うため**残置**。
- **null 安全化**：`show()` は撤去済みパネル ID を渡されても何もしない（潜在的エラー防止）。`renderAll()` は `renderMyView` 等の現役描画のみを呼ぶよう整理。
- 旧タブの描画関数（`renderTableA/B/C` `renderPersonView` `renderTabF` 等）と Tab C インライン編集系は**呼び出し経路から外れた死にコードとして残置**（共有ヘルパーと交錯しており物理削除は次回の任意作業）。
- **データ辞書**（`docs/data-dictionary.md`）を更新：🗑️＝削除可の凡例を追加し、各レガシー列の削除可否と削除手順を明記。

> ⚠️ 管理者作業：本版はフロントのみの変更で **Apps Script 再公開は不要**。任意で、シートのレガシー列／`組別頁面`・担当者シートを削除してよい（削除後はダッシュボード再読込で My Work 正常表示を確認）。

## v0.37.0 — 2026-06-01

**[リデザイン P2改] 「我的」を階層テーブルへ作り替え**（小沼氏のモックアップに準拠。カード型を廃止）

メイン画面を、Issue→Mission→Task を1行ずつ並べる**階層テーブル**に変更。列は **NO. / Issue / Mission / Task / Link / folder / DRI / 狀態 / 狀態紀錄 / 協作 / 戰略組擔當 / 記錄日** の12列。

- **NO.**：`#01 / #01-M01 / #01-M01-T01`（**表示だけ簡略化**。保存される編號は不変）。子行では親列をグレー表示で畳む。
- **名前フィルタ**：名前を選ぶと「**自分が DRI の行＋その上下層（親 Issue/Mission・子 Mission/Task）**」を表示。「看全部」で全件総覧。「顯示已結案…」で終了系も。
- **インライン／モーダル併用**：狀態チップ=クリックで7状態グリッド→即保存。狀態紀錄=**ダブルクリックで直接記入**（Mission→進度ログ追記／Task→進度／Issue→事務局備註）、Mission は**シングルクリックでログモーダル**。Task 名=**ダブルクリックで改名**。Issue/Mission 名=クリックで**編集モーダル**（定義も編集可）。
- **DRI／協作／戰略組擔當** は處カラーのチップ表示。**＋M／＋T** で Mission／Task を追加。**Link** は件数表示（Issue/Mission はクリックでリンク一覧）。
- **定義の編集**：新增/編集モーダルに **Issue定義／Mission定義** のテキスト欄を追加（`addMission`/`addIssue`/`updateMission`/`updateIssue` で保存。Apps Script の更新許容に `事務局備註` も追加）。
- **未対応（次回）**：⑥ folder（SharePoint 列）の保存、セル内での人物チップ直接編集、Task のリンク一覧モーダル、Task 単クリックの詳細モーダル。
- v0.36.0 のカード版 `renderMyView` は本版で置き換え（カード用 CSS は無害に残置）。

## v0.36.0 — 2026-06-01

**[リデザイン P2] 名前起点メイン画面「我的」を新設**（詳細は `REDESIGN-PLAN.md` §4.1）

「你是誰？」で自分の名前を選ぶと、自分に関わる Issue／Mission／Task が出る個人ビュー。タブの先頭・既定表示に。

- **DRI 上段（我負責）／協作 下段（我協作）**：自分が DRI（戰略負責人/擔當/Task DRI）の項目を Issue→Mission→Task の3層ツリーで上段に、協作（Task の協作欄に名前）を下段に。
- **インライン記入（ノーストレス）**：狀態はバッジクリックで7状態グリッド→即保存。Mission の「本週進度」は一行入力→記入（進度ログへ追記）。Task は進度を直接インライン編集。各 Mission 下に「＋ 新增 Task」。
- **空白の可視化**：各行に「上次：N 天前」（>7日 橙・>14日 赤）。
- **定義の常時表示**：Issue定義／Mission定義 をカード内に表示（カスケード健全性＝D8）。
- **Task一覽（3層目）読込**：`loadTasksLayer()`→`window.tasksByParent`。汎用 POST `_postWrite`（addTask/updateTask）。
- **7状態の表示統一**：`構想中/策劃中/待審核/進行中/結案/凍結/中止` の色を追加、旧 `未開始→構想中`・`需確認→待審核`・`完成→結案` を表示時に読み替え（`normalizeStatus`）。`statusBadge` 全体に適用。
- 旧 `Task一覽`＝Mission 別名フォールバックを**廃止**（Task一覽 は3層目の実体になったため）。
- 名前選択は localStorage 永続（`jig:myWho`）。既存タブ（A/B/C/F・詩雅/育菱・管理）は不変。
- ⚠️ Task の追加/編集・定義列表示には **Apps Script 再公開（cascade1）＋ setupJIG** が前提（P1 参照）。未実施でも Issue/Mission の表示・狀態/進度記入は動作。

## [リデザイン P1] Apps Script データモデル拡張 — 2026-06-01

**3層カスケード（Issue→Mission→Task）と7状態の器をバックエンドに用意**（詳細は `REDESIGN-PLAN.md`）。

- **Task一覽 タブ新設**：`編號 / Task / 親編號 / DRI / 協作 / 狀態 / 進度 / 更新日 / 連結`（3層目の実体）。`setupJIG` で作成、`狀態` プルダウン・色・更新日鮮度色を付与。
- **定義欄を追加**：`Issue主檔` に `Issue定義`、`Mission一覽` に `Mission定義`（カスケード健全性の言語化＝D8）。既存シートへは非破壊で追記。
- **7状態化**：`STATUS_VALUES = 構想中 / 策劃中 / 待審核 / 進行中 / 結案 / 凍結 / 中止`。旧 `未開始 / 需確認` は書込許容（移行期互換）、表示側の読み替えは P2 で実施。状態プルダウンは `allowInvalid:true` に変更（旧値が残る行を弾かない）。
- **doPost に `addTask` / `updateTask` 追加**：`_handleAddTask`（親 Mission 配下に `-K{n}` 自動採番）・`_nextTaskId`。`_handleUpdate` の許容フィールドに Task 系（Task/DRI/協作/進度/連結/定義）を追加。
- `doGet` 診断に `taskSheetExists` / `statusValues` を追加。`CODE_VERSION = gs-2026-06-01-cascade1`。
- **index.html は未変更**（ライブサイトは現状維持）。UI は P2 で対応。
- ⚠️ **管理者作業が必要**：①`setupJIG` を 1 回実行（`Task一覽` 作成・定義列追補）②Apps Script を**同一 URL で再公開**（`doGet` の `codeVersion` が `cascade1` になることを確認）。これが済むまで Task の追加/編集は動かない。

## v0.35.0 — 2026-06-01

**タイポグラフィのトークン体系を導入（一元管理の土台）**

これまでフォントサイズが 7〜28px の14種・ウェイトが各パーツ直書きで散在し、「一箇所変えても全体に効かない」状態だった。設計の一次ソースを `:root` に集約する。

- **サイズ・トークン**：`--fs-2xs(9) / --fs-xs(10) / --fs-sm(11) / --fs-ui(12) / --fs-base(13) / --fs-lg(15) / --fs-h(17) / --fs-xl(22)` の8段階に整理（密な表 UI の実寸に合わせたスケール）。
- **ウェイト・トークン**：`--fw-normal(400) / --fw-medium(500) / --fw-semibold(600) / --fw-bold(700)` の4語で意味づけ。
- **主要パーツを変数参照へ移行**：`body / h1 / .tab / 表 th・td / sec-row / person-chip / person-btn / 通知バー / ダイジェスト窓`。今後この長いテールも順次トークン化していく（gradual migration）。
- **CJK 修正**：v0.34.0 の本文 `font-weight:300`（Light）は繁体字の小サイズで線が霞み逆効果だったため **400 に復帰**。Light は CJK 本文では使わない方針。Noto Sans TC は維持。
- Apps Script 変更なし。

## v0.34.0 — 2026-06-01

**フォントを Noto Sans TC・本文ウェイト 300（Light）に**

- Google Fonts の Noto Sans TC / Roboto に `300` ウェイトを追加読込。
- `--font` / `--font-cjk` の先頭を `'Noto Sans TC'` に（繁中の表示を統一）。
- `body` の `font-weight` を 400→**300（Light）**に。見出し・バッジ・ボタン等の個別指定（600/700）はそのまま維持。
- Apps Script 変更なし。
- ※密度の高い小さなセル（10px）では Light が細く見える場合あり。読みづらければ本文 400 へ戻すか、セルのみ太めにする調整が可能。

## v0.33.1 — 2026-06-01

**ダイジェスト窓／編集モーダルを淡いブルーのガラスに**

- **ダイジェスト窓**（Mission進度ホバー）：背景 `rgba(238,245,255,0.74)`＋`--glass-blur`、青みの縁・影、角丸18px。区切り線とフッター文字も青系に。
- **編集モーダル**：v0.33.0 の白ガラスに薄い青み（`rgba(244,248,255,0.84)`＋青い内縁）を足して統一。
- Apps Script 変更なし。

## v0.33.0 — 2026-06-01

**リキッドグラス化（クロームのみ・可読性優先）**

iOS/macOS 26 の Liquid Glass に寄せて「枠まわり」をガラス化。データ密度の高い表本体は不透明のまま維持（可読性優先）。

- **淡いグラデ背景**：`body` に青〜緑〜暖色の固定グラデ＋`body::before` の放射グラデ。ガラスが透ける下地を用意。
- **縁の光（specular highlight）**：共通トークン `--glass-edge`（上辺の明るい内側リム＋下辺の微かな陰＋全周のガラス縁、inset 影で表現）と `--glass-blur` を導入。
- **屈折ハイライト**：`topbar::before` に左上光源の斜めグラデ。
- 適用先：**topbar / タブセレクタ（トラック＋thumb）/ 編集モーダル / 管理者ボタン**。不透明度を下げ（topbar 0.72→0.55 等）、角丸を拡大（スクワークル寄せ）。
- Apps Script 変更なし。

## v0.32.4 — 2026-05-31

**タブを iPhone 風スライドセグメンテッドコントロールに**

選択タブの白いピルが**滑らかにスライド**して移動する iOS UISegmentedControl 風に。`.tab-thumb`（絶対配置の白インジケータ・`cubic-bezier(.32,.72,0,1)` でアニメ）を `.tabs` に追加し、`moveTabThumb()` が選択タブの `offsetLeft/offsetWidth` に合わせて移動。`show()`／初期化／`resize` で更新。主タブ以外（D/E/Admin）表示中はインジケータを非表示。Apps Script 変更なし。

## v0.32.3 — 2026-05-31

**システムタイトル変更**

「TBBS-經營Issue管理系統」→ **「JIG — Mission Control」**（h1 とブラウザ `<title>`）。ロゴ「JIG」とバージョンバッジは据え置き。

## v0.32.2 — 2026-05-31

**フィルタ/検索行もスクロール固定**

ヘッダー行に加え、**フィルタ・検索行（`ctrl-bar`）も `position: sticky; top:0`** で固定。ctrl-bar の高さは可変（折返し）なため、`updateStickyOffsets()` で実測して CSS 変数 `--ctrlbar-h` にセットし、`thead th` の `top` をその分だけ下げて重なりを回避。`show()`／`renderAll()`／`window.resize`（rAF デバウンス）で更新。Apps Script 変更なし。

## v0.32.1 — 2026-05-31

**スクロール時にテーブルのヘッダー行を固定**

縦スクロールで列ヘッダーが見えなくなる問題に対応。`.panel` を**内部スクロール領域**（`overflow:auto` ＋ `max-height: calc(100vh - 150px)`）にし、`.panel table thead th` を **`position: sticky; top:0`** で上端固定（下線は `inset` シャドウで保持）。Tab A は `table.resizable th{position:relative}` に勝つようセレクタ特異度を上げて適用。横スクロール時はヘッダーも一緒に横移動（縦のみ固定）。Apps Script 変更なし。

## v0.32.0 — 2026-05-31

**macOS 風 UI 刷新 v1（基盤トークン＋チロム）**

方針：ライトのみ／フロストガラス使用／情報密度はコンパクト維持。

- **デザイントークン**（`:root`）：システムフォント（`-apple-system…`＋PingFang TC）、アクセント `--accent:#007AFF`、ヘアライン `--hairline`、`--shadow-sm/md/lg`、`--radius-card:12px`/`--radius-ctl:7px`。本文背景を `#F5F5F7` に。
- **トップバー**：sticky＋**フロストガラス**（`backdrop-filter: blur(20px)`＋半透明白）＋角丸＋ソフトシャドウ。
- **タブ**：Google カラーの折りタブ → **セグメンテッドコントロール**（グレー地・選択は白ピル＋影）。
- **パネル**：独立した**白カード**（角丸12px・ヘアライン・ソフトシャドウ）。
- **ボタン/入力/モーダル**：角丸・アクセント `#007AFF`・フォーカスリング・モーダルは大角丸＋ソフト影＋オーバーレイ blur。`+ 新增`・保存系をアクセント色に。
- データ表のグリッド/密度は据え置き（**v2 で精緻化予定**：ヘアライン化・行ホバー等）。Apps Script 変更なし。

## v0.31.2 — 2026-05-31

**読み込み中の表示にプログレスバー**

「正在從雲端載入資料…」のプレースホルダに**不確定プログレスバー**（左→右へスライドする青いバー、`.load-bar` / `@keyframes loadslide`）を追加。読込中であることが視覚的に伝わる。Apps Script 変更なし。

## v0.31.1 — 2026-05-31

**お知らせボード（公告）を新設**

トップ（タイトルと管理者ボタンの間）に**公告ボード**を設置。全員に**読み取り専用**で表示、**編集は管理者專用ページの「公告板」**から。

- 新タブ **`公告`**（`訊息 / 更新日`、1 件を 2 行目に保持）。`setupJIG` で作成。
- Apps Script：`doPost` に **`saveAnnounce`**（2 行目 upsert）。`doGet` に `announceSheetExists`。`codeVersion=gs-2026-05-31-announce1`。
- クライアント：`loadAnnounce()`→`window.announceText`、`renderAnnounceBoard()`（空なら非表示）。管理者ページに公告テキストエリア＋儲存/清除。ベルアイコンつきの控えめな黄色ボード。
- **要再公開＋setupJIG**。

## v0.31.0 — 2026-05-31

**詩雅／育菱の呼び出しを「▶」格納式に**

管理者專用ページ内に置いた見出し・説明文・バッジを全廃し、**小さな「▶」トグル**のみに。クリックで展開（▶→▼回転）し、中に詩雅／育菱の小さなボタンが現れる。普段は記号だけでほぼ存在感ゼロ。Apps Script 変更なし。

## v0.30.9 — 2026-05-31

**詩雅／育菱の呼び出しを管理者專用ページ内へ移設**

topbar に置くと（小さくしても）かえって押したくなり目立つため、**topbar から撤去**し、`管理者專用` パネル内に **「事務局：個人檢視（詩雅・育菱）」** セクションとして格納。社員の動線からは見えず、本人は管理者ページ経由で呼び出せる（§7 の「UI 隠蔽」レベル）。Apps Script 変更なし。

## v0.30.8 — 2026-05-31

**詩雅／育菱の呼び出しボタンを控えめに**

社員に目立たせないよう、上部の `👤 詩雅`／`👤 育菱` を**小さな人物 SVG アイコン＋極小の名前**へ。普段は枠なし・薄いグレー・低 opacity でほぼ溶け込み、**hover／選択時のみ**色づく（詩雅＝赤・育菱＝紫）。本人は名前で見分けて呼び出せる。

## v0.30.7 — 2026-05-31

**個人備註モーダル：進度ログが見えない問題を修正（CSS）**

進度ログは描画済み（`htmlLen=604`）なのに表示されない＝モーダル本体の flex 縦並びで下のテキストエリアに押され、**ログ枠の高さが 0 に潰れて**いた（中身は overflow で隠れる）。`#memoLog` に **`flex:0 0 auto`**（潰さない）＋`overflow-y:auto` を付与し、最大 180px で内部スクロール表示するよう修正。

## v0.30.6 — 2026-05-31

**個人備註モーダル：進度ログ表示の修正＋親Issue 名**

- 進度の歷來記錄が空に見える問題を修正。現況テキストの参照を一覧（Tab D/E）と統一し **`Mission進度 || 事務局備註`** をフォールバックに（Mission進度 が空で 事務局備註 に文がある Mission でも表示される）。
- **親Issue 名**を表示（従来は親編號バッジのみ）。ヘッダ直下に「親編號バッジ＋Issue 名」→ その下に Mission 名の 2 段表示。

## v0.30.5 — 2026-05-31

**個人備註モーダルに Mission 名称＋進度ログ（読み取り専用）**

メモ/留言を書く際の文脈として、モーダルに以下を追加：

- **Mission 名称**（親Issue 編號バッジつき）をヘッダ直下に表示（従来は編號＋姓名のみ）。
- **Mission 進度の歷來記錄**（`window.missionLog` の時系列、進度編集モーダルと同じ見た目・**読み取り専用**）。ログが無ければ現況テキストを移行表示。
- ログ HTML を `_missionLogHtml()` に共通化（`renderMissionLog` も使用）。Apps Script 変更なし。

## v0.30.4 — 2026-05-31

**列幅保存の自動修復（詩雅タブの右端はみ出し）**

`私的備註` 列追加で列数が 10→11 になったが、`localStorage`（`jig:colw:panel-D` 等）に**古い10列時代の幅**が残り、詩雅タブだけ右端が切れていた（育菱は保存値が無く正常）。`loadColWidths`/`saveColWidths` に**列数 `__n` を記録**し、現在の列数と異なれば**保存値を破棄**して既定幅に戻すよう修正（列の増減があっても自動で修復）。Apps Script 変更なし。

## v0.30.3 — 2026-05-31

**絵文字 → 洗練されたインライン SVG アイコン**

チープに見える絵文字（★ ☆ 📝 💬 ⭐ ✎）を、**Feather 風のインライン SVG ラインアイコン**（`currentColor` 追従でテーマ色に馴染む）へ置換。`svgIco(name,size)` ヘルパー＋`_SVG` パス定義を追加。

- アイコン体系：**重要＝星（塗り）／私的備註＝doc（file-text）／小沼留言＝message／編輯＝pencil／追加＝plus**。Tab D/E の「備」列・サマリー・重要清單ボタン＆モーダル・メモモーダル見出し・各行 ✎ に適用。
- Apps Script 変更なし（再公開不要）。※検索プレースホルダの 🔍 や管理者リンクの 📝 等は対象外（必要なら別途一掃可）。

## v0.30.2 — 2026-05-31

**個人備註を拡張：★重要フラグ ＋ 小沼からの留言**

`個人備註` タブを `編號 / 姓名 / 備註 / 留言 / 重要 / 更新日` に拡張（`setupJIG` か `saveMemo` 初回で列を追補）。

- **★重要フラグ（個人別）**：Tab D/E の各行の ★ でトグル（楽観的更新→失敗時に戻す）。`只看 ★ 重要` フィルタ、`⭐ 重要清單` モーダル（フラグ Mission をチェック一覧・外すと解除・名前クリックで編集）。サマリーに `★ 重要 N` バッジ。
- **小沼からの留言（本人タブ宛）**：メモモーダルに「來自小沼的留言」欄を追加。本人のタブにのみ表示（💬 アイコン）。
- 備註列を「★ ＋ 📝/💬」アイコン列（幅 62px）に。`saveMemo` は届いたフィールド（備註/留言/重要）だけ部分更新。
- `codeVersion=gs-2026-05-31-memo2`。**要再公開＋setupJIG**（既存タブへ 留言/重要 列を追加）。

## v0.30.1 — 2026-05-31

**Tab D/E：私的備註を細いアイコン列に**（横はみ出し解消）。

## v0.30.0 — 2026-05-31

**個人備註（詩雅／育菱の私的メモ）**

詩雅／育菱が各 Mission に**自分用のメモ**を書けるように。タブ＝本人なので記入者はタブで自動判定。本人のタブにのみ表示（他人の画面には出さない）。※静的サイト＋共有シートのため、シート/ソースを見れば技術的には見える＝厳密非公開ではない（§7）。

- **新タブ `個人備註`**（`編號 / 姓名 / 備註 / 更新日`）。`setupJIG` で作成、または最初の保存時に自動生成。
- Apps Script：`doPost` に **`saveMemo`**（`編號`＋`姓名` で upsert・上書き可）。`doGet` に `memoSheetExists`。`codeVersion=gs-2026-05-31-memo1`。
- クライアント：`loadPersonalMemos()`→`window.personalMemo`。Tab D/E に **「私的備註」列**（クリックで `📝 個人備註` モーダル編集・進行バー・Ctrl+Enter 保存）。本人のメモのみ表示。
- **要再公開＋setupJIG**：`setup-sheet.gs` 変更のため再デプロイ。タブは setupJIG か初回保存で作成。

## v0.29.2 — 2026-05-31

**Tab D/E：本文は Mission のみに（Issue 混在をやめる）**

Issue 行が Mission と同列に混ざり「Mission の一種」に見えて紛らわしかったため、**本文の一覧は Mission のみ**に戻した。關わる Issue は**サマリーに「關聯 Issue N」**として件数のみ表示（需對應・處グループ・✎編集・サマリーバッジ・ヒット判定修正はそのまま）。

## v0.29.1 — 2026-05-31

**Tab D/E：親Issue を毎行フル表示**

處グループ＋Issue 混在になったことで、従来の「同じ親なら空白」(省略表示) が分かりにくくなったため**廃止**。各 Mission 行に**親Issue を毎行表示**（`makeParentInfo(..., null)` で dedup 無効化）。Issue 行は自身が上位のため親列は空。

## v0.29.0 — 2026-05-31

**詩雅／育菱タブ（Tab D/E）を事務局向けに刷新**

中心メンテナ二人の作業性を上げる改修。

- **ヒット判定の修正（重要）**：完全一致をやめ `personMatches`（`splitPersons`＋短縮/フル名照合）に。**複数担当（"詩雅/育菱"）や新フルネーム（"張育菱"）も漏れずヒット**。`setupTabDEFilters` も同関数に統一。
- **「需對應」セクションを上部に**：`需確認` もしくは `停滞>7日` を先頭にまとめ、停滞日数の降順で表示（催促の起点）。
- **その場で編集**：各行に **✎**（`openEditEntryModal`）、**狀態セルのクリックで編集モーダル**を開く（`simpleRow` に `showEdit`）。
- **關わる Issue も表示**：従来 Mission のみ → その人が `擔當`／`戰略負責人` の **Issue も対象**に。行に `ISSUE` バッジ。
- **處ごとグループ＋件数サマリー**：需對應の下を `處` 別（色マップ順）にグループ化。最上部に `合計 / Issue / Mission / 進行中 / 需確認 / 停滯>7天` のサマリーバッジ。
- Apps Script 変更なし（再公開不要）。

## v0.28.2 — 2026-05-31

**Issue にも擔當チップを追加**

Issue フォームに `擔當` 列（Issue主檔 H 列）の入力が無く選べなかった。Issue の追加・編集フォームに **擔當 チップピッカー**（全員・複数選択可）を追加。`組` を独立行にし、`擔當｜戰略負責人` を 1 行に整理。

- クライアント：`addIOwner` ピッカーを追加（init／編集事前入力／送信／配線）。Issue の add・update ペイロードに `擔當` を追加。
- Apps Script：`_handleAddIssue` の書込みマップに `擔當` を追加（`_handleUpdate` は既に対応済み）。`codeVersion=gs-2026-05-31-edit3`。**要再公開**。

## v0.28.1 — 2026-05-31

**編集の `not_found` 修正**

`updateIssue` が `儲存失敗：not_found` で失敗。`_handleUpdate` が編號を **A 列決め打ち**で探していたが、`Issue主檔` は編號が **C 列**（A=區分/B=軸/C=編號）のため一致せず not_found だった。

- 編號の列を**ヘッダ名 `編號` で特定**するよう修正（Mission一覽=A／Issue主檔=C の両対応）。
- `codeVersion=gs-2026-05-31-edit2`。**要再公開**。

## v0.28.0 — 2026-05-31

**既存 Issue／Mission をダッシュボードから編集・一覧の人物をチップ表示**

- **行の ✎ で編集**：Tab A の各行（Issue／Mission）に ✎ ボタン（ホバーで表示）。追加モーダルを**追加／編集 兼用**にし、該当行を事前入力して開く。種別トグルは編集時は隠す。
- **編集できる項目**：名称（Issue名／Mission名）・**擔當／戰略負責人（チップピッカー：複数選択・×削除・戰略負責人は4名限定）**・狀態・Issue の 處／組（連動）・Confluence URL。**編號は変更不可**（表示のみ・グレー）。Mission の親 Issue も固定。
- **一覧の `擔當`／`戰略負責人` を處カラーのチップ表示**に（`personChipsHtml`／`buildPersonDeptMap`、renderAll でキャッシュ更新）。短縮名でも行の出現から處を推定して配色。Tab A／Tab C／Tab D・E に適用。
- Apps Script：`doPost` に **`updateMission`／`updateIssue`** を追加（編號で行特定 → 許可フィールドのみ反映、数式列は触らず・入力規則違反はスキップして warnings）。`_updateRowByHeaders`／`_handleUpdate`。進度ログ・編號・親編號は対象外。`codeVersion=gs-2026-05-31-edit1`。
- **要再公開**：`setup-sheet.gs` 変更のため Apps Script を同一 URL で再デプロイ。`codeVersion` が `edit1` であることを確認。

## v0.27.4 — 2026-05-31

**新增ボタンの処理中表示を進行バーに**

- スピナー（`is-loading`）をやめ、ボタン上を**左→右に満ちる進行バー**（`is-progress`、約6.5s で `forwards` 保持）に変更。編集モーダルの「伸びるバー」と同じ思想で、フリーズと誤解されにくく進行感が出る。
- テキスト「新增中…」は `<span>`（`z-index:1`）で前面に保持、バー（`::before`, `z-index:0`）は背面で伸長。

## v0.27.3 — 2026-05-31

**戰略負責人の候補を 4 名に限定**

- `戰略負責人` の選択肢を **小沼和幸・王詩雅・張育菱・陳少琪** の 4 名のみに限定（`STRATEGIC_LEADS` 定数で管理。変更は 1 か所）。`擔當` は全員のまま。
- 名前ピッカーに**候補限定リスト**機能を追加（`resetNamePicker(prefix, val, multi, allow)`／`window._namePickAllow`）。限定時は名前から直接チップを構築し、名簿未ロードでも 4 名が必ず表示される。

## v0.27.2 — 2026-05-31

**戰略負責人も複数選択に・選択タグに「×」個別削除**

- **戰略負責人を複数選択対応**（Mission・Issue 両フォーム）。擔當と同じく `/` 区切りで保存。`resetNamePicker(..., true)`。
- 名前ピッカーのトリガーに並ぶ**選択タグに「×」**を追加。クリックでその名前だけ削除（メニュー開閉は発火させない＝`stopPropagation`）。メニューが開いていればチップの active 表示も同期更新。

## v0.27.1 — 2026-05-31

**擔當の複数名選択に対応**

データ層は以前から複数対応（`擔當` は `/` 等の区切りで複数名、`splitPersons` で分割）だったが、追加フォームの入力 UI が単一だった。名前チップピッカーを**複数選択モード**に拡張。

- 選択状態を配列で保持（`window._namePickSel`）、`multi` フラグで複数/単一を切替（`resetNamePicker(prefix, val, multi)`）。**擔當=複数可**、戰略負責人=単一。
- 複数モードはチップ再クリックでトグル、メニューは開いたまま連続選択可。トリガーは選択名を**処カラーのタグで並べて表示**。
- 保存時は `namePickValue()` が選択を **`/` 区切り**で結合して送信（既存データ表記と一致）。

## v0.27.0 — 2026-05-31

**新增モーダルの UI/UX 改善 ＋ 表記を繁體中文に統一**

- **言語方針**：ツール内の表示テキストは**すべて繁體中文**に統一（利用者は台湾スタッフ）。新增モーダルに残っていた日本語（セグメント切替・ラベル・プレースホルダ・トースト・編號重複警告 等）を繁中化。以後の新規 UI も繁中で実装する。
- **擔當／戰略負責人を名前チップピッカー化**：自由入力／datalist をやめ、記入者チップと同じ**處ベース配色のチップ選択**に統一（`setupNamePicker`/`renderNamePickerChips`、状態は `window._namePick`）。再クリックで解除（任意項目）。
- **Issue の「組」を「處」連動プルダウン**に：選んだ `處` 配下の `組`（既存 Issue ＋ 人員マスタから収集）を `組` セレクトに動的表示（`_groupsForDept`/`_populateAddIGroups`）。
- **新增ボタンに処理中スピナー**：押下で `is-loading`（回転スピナー＋無効化）になり、フリーズと誤解されないように。
- Apps Script 変更なし（再公開不要）。

## v0.26.4 — 2026-05-31

**追加の入力規則対応を完全化（行は作られるのに「失敗」表示になる不整合を解消）**

v0.26.2 の try/catch は `setValue` を包んでいたが、**データ入力規則違反は `setValue` の瞬間でなく後続の `flush()`（ループ外）で発火**するため catch をすり抜け、`exception` で失敗応答になっていた（実際には行は作成済み）。`Confluence URL` 列に入力規則（ドロップダウン）があり、自由入力 URL が弾かれていたのが今回の引き金。

- `_appendByHeaders`：各セルの `setValue` 直後に **`SpreadsheetApp.flush()` を try 内で実行**し、入力規則違反をその場で捕捉。弾かれた列は `clearContent` でクリアして `rejected` に記録し、行作成とレスポンス `ok:true`（warnings 付き）を保証。
- `doGet` の `codeVersion` を **`gs-2026-05-31-addrow4`** に更新（再公開の反映確認用）。
- **要再公開**：`setup-sheet.gs` 変更のため Apps Script を同一 URL で再デプロイ。反映確認は Web App URL を開いて `codeVersion` が `addrow4` であること。

## v0.26.3 — 2026-05-31

**重大：ダッシュボード全体フリーズを修正（gviz ヘッダー誤検出）**

`Issue主檔` の読込で `rowsToData` が「缺少必要欄位：區分,軸,編號,Issue」で例外 → `loadFromSheet` が握りつぶし、プレースホルダーのまま全体停止していた。原因は **gviz がヘッダーを2行と誤検出してヘッダー行とデータ行を結合**（人員タブの二重ヘッダーと同型）し、`區分` 等が `區分 戰略` のように崩れていたこと。

- gviz の CSV URL **5 箇所すべてに `&headers=1`** を付与し、ヘッダーを必ず1行に固定（誤検出の根絶）。Apps Script 変更なし＝**再公開不要**。
- 防御として `rowsToData` を **「先頭行に必須列が無ければ先頭数行から `編號`＋`Issue` を含む行をヘッダーとして探す」** 方式に強化。ヘッダーが多少ズレても読めるようにした。
- 備考：`Issue主檔` 末尾の重複 `事務局備註` 列は本件の直接原因ではない（掃除推奨：余分な列を削除可）。

## v0.26.2 — 2026-05-31

**追加機能をデータ入力規則に強く（Issue 追加失敗を解消）**

Issue 追加が `exception: セル M37 …データ入力規則に違反` で失敗していた。Issue主檔 の `狀態` 列に「無効値を拒否」する入力規則があり、フォームの5段階値が（Issue は3段階のため）弾かれて `setValue` が例外化、行作成ごと失敗していた。

- `_appendByHeaders` の各列書き込みを **try/catch** で包み、入力規則等で弾かれた列は **スキップして行作成は継続**。弾かれた列名を `rejected` として収集し、応答 `warnings` で返す。
- クライアントは `warnings` があれば「一部項目はシートの入力規則によりスキップ」と通知。Issue／Mission 本体の作成は成功する。
- **要再公開**：`setup-sheet.gs` 変更のため Apps Script を**同一 URL で再デプロイ**。

## v0.26.1 — 2026-05-31

**重大バグ修正：Mission/Issue 追加が数式列を壊す問題**

v0.26.0 の append が、`親編號` などを **ARRAYFORMULA 等の数式で自動算出**しているシートで、新規行の数式列に値を直接書き込み、列全体を `#REF!` で破壊 → 既存 Mission が親リンクを失い一覧から消える不具合があった。

- `_appendByHeaders` を**数式に優しい方式**へ変更：(1) アンカー列 `編號` を先に書いて数式列を自動算出させ、(2) **数式由来／自動算出済みの列はスキップ**し、空の手入力列にだけ値を書く。これにより「親編號が数式」「Confluence URL が数式」のどちらのシート設計でも壊さない。手入力列の場合は従来どおり値を書く。
- **要再公開**：`setup-sheet.gs` 変更のため Apps Script を**同一 URL で再デプロイ**。

## v0.26.0 — 2026-05-31

**ダッシュボードから Issue／Mission を追加（事務局の構造管理を画面内に）**

事務局がシートを直接開いて行追加していた作業を、ダッシュボード内の最小フォームに集約（目的B＝事務局の負荷低減）。既存の更新経路は変更せず、append アクションを「足すだけ」。

### ダッシュボード（index.html）
- Tab A（Issue & Mission）ツールバー右端に控えめな **「＋ 新增」** ボタン。
- **新增モーダル**（Issue／Mission をトグル）。最小項目固定・詳細は Confluence へ誘導（フィルタ②③遵守）。
  - *Mission*：親 Issue を選択 → **編號を自動採番（親編號-M{n}）してプレビュー**、Mission名、擔當、戰略負責人、狀態、Confluence URL。處/組/會議等は親から継承（入力不要）。
  - *Issue*：編號（手動＋**重複ライブチェック**）、Issue名、處（色マップ7部門から選択）、組、戰略負責人、狀態、Confluence URL。
- 保存は `action` 付き POST → 成功後に `loadFromSheet()` で再読込し、継承列込みで確実に反映。背景スクロールロック・Esc/Ctrl+Enter 対応。

### Apps Script（setup-sheet.gs）
- `doPost` に **`action:'addMission'` / `action:'addIssue'`** を追加。`_appendByHeaders`（ヘッダ名で列特定し1行 append）・`_nextMissionId`（親配下の次番号採番）を新設。Mission は親存在チェック、Issue は編號一意チェック。`ISSUE_SHEET_FOR_WRITE='Issue主檔'` 追加。
- **要再公開**：`doPost` 変更のため、Apps Script を**同一 URL で新バージョンとして再デプロイ**（§9）。未実施だと追加 POST は旧コードに弾かれる（更新機能は従来どおり）。

## v0.25.3 — 2026-05-31

**編集モーダルのスクロール裏抜けを修正**

モーダルの中身が短く（ログが少なく）スクロール不要なとき、ホイール操作が背後のページに伝わってスクロールしてしまう現象を解消。

- 編集モーダルを開いている間、`<body>` に `modal-open` クラスを付与し `overflow: hidden` で背景スクロールをロック（`openEditModal` で付与、`closeModal` で解除）。
- `.remark-modal-overlay` と `.remark-modal-body` に `overscroll-behavior: contain` を追加し、モーダル内スクロールが端に達してもページへ連鎖しないようにした。

## v0.25.2 — 2026-05-31

**記入者の既定値を「王詩雅」に**

- `DEFAULT_AUTHOR = '王詩雅'` を新設。編集モーダルを開いたときの既定の記入者を **王詩雅** に変更。優先順位は「**前回選択（`localStorage`）→ `DEFAULT_AUTHOR`**」。Mission の `擔當` を既定にする旧挙動は廃止（既定値が上書きされないように）。既定を変えたいときは `DEFAULT_AUTHOR` の1か所を編集。
- `resolveRosterName()` を**双方向マッチ**に拡張（名簿名が略称を含む／既定値が名簿の略称を含む、どちらでも解決）。名簿が未ロードのフォールバック時でも `王詩雅 ↔ 詩雅` のような揺れを吸収。

## v0.25.1 — 2026-05-31

**記入者チップの仕上げ（小型化・配色・既定選択の修正）**

人員マスタ導入後の運用フィードバックを反映。

### ダッシュボード
- **チップ小型化**：`font-size 12→11px`・余白 `3px 11px → 2px 8px`・`gap 5→4px`・枠 `1.5→1px`、メニュー高さ `240→320px`。同じ画面でより多く表示し、スクロールを抑制。
- **`總管理` の色をグレー→ピンク**（`dept-c-pink`：bg `#FDE7F3` / fg `#B80672`）に変更。グレーは「色マップに無い真に不明な部門」のフォールバック専用に戻した（「いない人」に見える問題を解消）。一覧表セル（`td.dept-c-pink`）・Mission 行文字色（`mc-pink`）・編號バッジも追従。
- **既定の記入者を名簿のフルネームに解決**：`resolveRosterName()` を追加し、Mission の `擔當` 略称（例「育菱」）や `localStorage` の旧値を、名簿のフルネーム（例「張育菱」）へ末尾一致／部分一致で解決。名簿運用時は**名簿に無い名前で幽霊チップを末尾に作らない**よう修正（重複表示の解消）。フォールバック時のみ従来どおり先頭補完。

## v0.25.0 — 2026-05-31

**人員マスタ（名簿の一次ソース）を導入 → 記入者チップに處ベースの色**

これまで記入者チップに色が付かなかった原因は、人→組/處の**紐づけデータがシートに無かった**こと。色の基準を「組（17種・多対多）」から **「處（部門・7種）」** に変更し、名簿の一次ソースとして **`人員` タブ**を新設した。

### スプレッドシート / Apps Script
- 新タブ **`人員`**（`姓名 / 處 / 組 / 顯示順`）。`setupJIG` で自動作成。
- `doGet` 診断に `personSheetName` / `personSheetExists` を追加。

### ダッシュボード
- `人員` タブを gviz 読み込み（`loadPersonMaster()` → `window.personMap`）。
- 記入者ドロップダウンの**名簿・色・並び順を人員マスタから生成**。色は處ベースで、**一覧表の部門色（dept-c-*）と完全一致**。同じ處の人が色で固まって並ぶ。
- `人員` タブが**無い／空のときは従来どおり**（Mission の擔當/戰略負責人から名前収集・色なし）にフォールバック。

> **管理者の作業**：①Apps Script を再公開 → ②`setupJIG` 実行で `人員` タブ作成 → ③`人員` タブに名簿（姓名＋處）を貼る。貼った瞬間に記入者チップへ色が付く。處が空の人はグレー。

---

## v0.24.3 — 2026-05-31

**記入者を「ドロップダウン式」に変更（全社展開を見据えた省スペース化）**

- 記入者を**常時チップ表示 → ボタン1つ**に。クリックで下にメニューが展開する方式へ。
- メニューに**🔍 検索窓**を追加（名前で絞り込み）。人数が増えても探せる。
- メニュー内は従来どおり**組カラーのチップ**（同じ組が隣接）。チップ選択で自動で閉じる。
- 閉じたボタンには**選択中の名前＋その人の組カラー**を反映。
- 外側クリック・Esc でメニューだけ閉じる（モーダルは閉じない）。

---

## v0.24.2 — 2026-05-31

**編集モーダルの UI 改善（運用フィードバック反映）**

- **記入者を組カラーのチップで選択**：プルダウンを廃止し、人名を**組（L/Z/M/C/X/S/IP/共通）の色付きチップ**で表示。同じ組が隣接するよう並べ、組の色で探しやすく。選択中はチップが濃く＋枠線。前回の記入者・その Mission の擔當を既定で選択。
- **Confluence URL の折りたたみ**：リンクが 3 件を超えると初期で畳み、「▸ 展開 N 個連結」トグルで開閉。**多リンクでも下の歷來記錄が埋もれない**。
- **フォントを Noto Sans TC に統一**：記入者・進度入力欄・ログ本文に `--font-cjk`（Noto Sans TC 優先）を適用。日本語＋繁体字混在時の見栄えを改善。
- 進度入力欄の高さを少し下げ、一行運用に合わせて調整。

> ※ 5 番（「先前記錄」が消える問題）は **Apps Script v0.24.1 の再公開で解決**（既存の Mission進度 を「（既存）」としてログへ seed する救済が未デプロイのため。再公開後の初回記入から保全される）。

---

## v0.24.1 — 2026-05-31

**進度ログの微調整（運用フィードバック反映）**

- **日時の文字化け修正**：gviz が日時を Google シリアル値（例 `46173.04…`）で返すケースを日時文字列へ変換（`sheetSerialToStr()`）。あわせて Apps Script 側もログの日時を**文字列で保存**するよう変更（シリアル化を回避）。
- **既存 Mission進度 をログへ移行表示**：あるミッションの初回ログ追記時、既存の現況（H列）が残っていれば「（既存）」として過去分 1 行を seed してから新規を追記（過去の内容が消えない）。
- **ログ本文を小さめに**（12px・落ち着いた色）して「ログらしさ」を強調。
- **「進度說明」プレースホルダを薄く・小さく**。
- **フォント統一**（記入者プルダウンに `font-family: inherit`）。
- **狀態カラーの差し色**：編集モーダルの上縁に現在の狀態色（未開始/策劃中/需確認/進行中/結案）を表示。狀態ボタン切替で即座に色が変わる。

> ⚠️ Apps Script を変更（日時の文字列保存・既存内容の seed）。**シート側を反映するには Apps Script の再公開が必要**（同一 URL で新バージョン）。ダッシュボードの日時表示はフロント側変換で再公開前でも正しく出る。

---

## v0.24.0 — 2026-05-31

**進度を「追記ログ」化（本人週次記入／CLAUDE.md §1.5 路線転換の実装）**

これまで Mission進度は上書きで過去が消えていた。本版から、本人が週1で書く進度を**追記専用ログ**として蓄積し、過去の歩みを時系列で追えるようにした。

### スプレッドシート / Apps Script（`scripts/setup-sheet.gs`）
- 新タブ **`Mission進度ログ`**（`編號 / 日時 / 擔當 / 進度`）を追加。`setupJIG` 実行で自動作成（未作成なら保存時にも自動生成）。
- `doPost` に **`進度`（新規一行）＋ `擔當`（記入者）** の処理を追加：ログへ 1 行 append し、`Mission一覽` の現況（Mission進度 H列）へ最新行を**ミラー**、更新日を自動更新。応答に `logEntry` を返す。
- `doGet` 診断に `logSheetName` / `logSheetExists` を追加。

### ダッシュボード（`index.html`）
- 編集モーダルを刷新：**記入者プルダウン**（シートの擔當/戰略負責人から自動生成・前回値を記憶）＋**「今週の進度」入力欄**（常に空欄から追記）＋**「これまでの記録」タイムライン**（新しい順、最新を強調、ログ未蓄積時は現況を移行表示）。
- 追記ログタブを gviz で読み込み（`loadMissionLog()` → `window.missionLog`）。保存時は楽観的にタイムライン・現況へ即時反映。
- 読み取り専用の表示モーダルでは入力欄を隠し、タイムラインのみ表示。

> **本人の運用に必要な管理者作業**：①`setupJIG` を 1 回実行して `Mission進度ログ` タブを作成、②Apps Script を同一 URL で再公開（§9）。記入者の本格的な認証は次フェーズ。

---

## v0.23.0 — 2026-05-31

**Issue & Mission タブの件数表示に Mission を追加**

- これまで件数表示（`顯示 X / Y 筆`）は **Issue 行のみ**をカウントしていた。Mission 行はフィルタで表示/非表示を切り替えていたが件数に反映されていなかった
- 表示中の Mission 行と Mission 総数をカウントし、**`顯示 Issue X / Y · Mission X / Y 筆`** 形式で Issue と Mission を分けて表示
- フィルタ無効時（検索・部門・組・担当・結案トグルがすべて未指定）は従来どおり非表示

---

## ドキュメント更新 — 2026-05-31

**Apps Script 再デプロイ完了を反映（コード変更なし）**

- 旧 §10 TODO①（「Confluence URL 編集時の `更新日` 自動更新」「応答 JSON に時刻 `HH:mm` を含める」）はデプロイ済みで**完了**。`scripts/setup-sheet.gs` のサーバー実装（L179-206）がそのまま稼働。
- `index.html` の `ensureEditTimestamp()` は**無害な防御的フォールバックとして残置**（引数に時刻があればそのまま返すため、時刻入り応答は素通り）。撤去は任意。
- CLAUDE.md §10 の TODO 記述と作業ブランチ名（`claude/stoic-fermat-McakE`）を実態に合わせて更新。

---

## v0.22.1 — 2026-05-31

**Confluence 列ヘッダーを favicon に**

- Issue & Mission タブの Confluence 列ヘッダーを「Confluence」テキストから favicon アイコンに変更（他タブと統一）
- `conf-th-icon` の CSS スコープを `.simple-table th` から汎用 `th` に広げ、`#tableA`（Issue & Mission）にも適用
- 列幅を 160px → 54px に詰め、`.conf` セルを中央寄せ（アイコンのみ表示に合わせて整形）

---

## v0.22.0 — 2026-05-31

**Issue & Mission の Confluence 列：アイコンのみ＋複数はリンク一覧モーダル**

### 変更内容

- Issue & Mission タブの Confluence セルから**ラベルテキストを撤去し、アイコンのみ**に（D/E タブと同じスッキリ表示）
- リンクが**複数あるセルはクリックで小さな「Confluence 連結」モーダル**を開く（各リンクをラベル付きで一覧、クリックで別タブ表示）
- **単一リンクは従来どおり直接リンク**（モーダルを挟まない）
- 件数バッジ（`.conf-multi`/`.conf-badge`）を汎用 CSS 化して `.conf` と `.conf-icon` で共通化
- D/E タブの Confluence セルも同じ挙動に統一（単一→直接・複数→モーダル）

---

## v0.21.0 — 2026-05-31

**ダイジェストテキストウィンドウ（Mission進度 のホバー大表示）**

### 変更内容

- Issue & Mission / D / E タブの Mission進度 セルにマウスを乗せると、約 350ms 後に
  **画面中央へ大きく本文を表示**するプレビュー（ダイジェストテキストウィンドウ）を追加
- モーダルを開かずに全文を読める（一覧時の閲覧負荷を低減）
- Mission 番号・名前のヘッダー付き、本文は改行保持・読みやすい字間
- `pointer-events: none` で操作を邪魔しない。**クリックは従来どおり表示モーダル**が開く
- 小さいネイティブ tooltip（全文）は重複するため短い操作ヒント「點擊檢視詳情」に変更
- モーダル表示中・スクロール時はプレビューを抑制／非表示

---

## v0.20.0 — 2026-05-31

**他タブの Confluence 複数リンク表示＋読み取り専用「表示モーダル」**

### 1. Confluence セルの複数リンク対応（Issue & Mission / D 詩雅 / E 育菱）

- これまで他タブの Confluence セルは `Confluence URL` を単一リンクとして描画していたため、
  複数リンク（`名前|URL` 改行区切り）が壊れていた
- `confluenceCell()`（Issue & Mission タブ）を複数対応に書き換え：先頭リンク＋ラベル＋`+N` バッジ、tooltip に全件ラベル
- `simpleRow()`（D/E タブ）の Confluence セルを `confEditCell(..., false)` 経由に統一（1 アイコン＋件数バッジ）

### 2. 読み取り専用「表示モーダル」

- 他タブの Mission進度 セルをクリックすると、これまではタブ間ジャンプだったが、
  その場に**読み取り専用モーダル**を表示するように変更
- 狀態・Mission進度・Confluence リンクをまとめて閲覧可能（リンクは ↗ で開ける）
- フッターの **✎ 編輯** ボタンで Mission進度 タブへ移動し、その行の**編集モーダル**を自動で開く
- 既存の編集モーダルを `readOnly` モードで再利用（入力不活性化・編集系 UI 非表示）

---

## v0.19.0 — 2026-05-31

**Confluence リンクに手書きの名前を付けられるように**

### 背景

v0.18.1 の URL 自動抽出ではページの中身まではわからず（`頁面 161808493` 等）、
飛び先の判別に役立たなかった。事務局が任意の名前を手入力できるようにする。

### 変更内容

- モーダルの各 Confluence URL 行に**名前入力欄**を追加（URL の上）
- 名前未入力時はプレースホルダに URL 自動抽出ラベルを表示（何が入るか予告）
- 保存形式：1 セル内に 1 行 1 リンクで `名前|URL`。名前なしは `URL` のまま（**後方互換**）
- `parseConfEntries()` / `serializeConfEntries()` を追加。`parseConfUrls()` は URL のみ返すよう内部実装を変更（呼び出し側は無改修）
- 一覧 🔗 列の tooltip・モーダルは「手書き名 ＞ URL 自動抽出」の優先順でラベル表示
- `名前|URL` 判定は `|` の後ろが `http(s)://` のときのみ（URL 単体の誤判定を回避）

### 注意

- スプレッドシート側のセルも `名前|URL` 形式で保存される。事務局が直接編集する際もこの形式（名前なしは従来どおり URL のみで可）。

---

## v0.18.1 — 2026-05-31

**Confluence URL からページ名を自動抽出してラベル表示**

### 背景

生 URL が並ぶだけでは「どのページに飛ぶか」判別できなかった。静的サイトのため
Confluence API は叩けないが、URL 自体にスペースキー・ページタイトル（スラッグ）・
アンカー（#編號）が埋め込まれているので、そこから読めるラベルを生成する。

### 変更内容

- `confLabel(url)` を追加：URL から `[スペースキー] ページタイトル #編號` を抽出
  - 例：`/pages/614694913/JIG_LTV` → `[JIGissue] JIG_LTV`
  - 例：`/pages/.../JIG_#%E6%88%B0%E7%95%A5-1` → デコードして `JIG_ #戰略-1`
  - スラッグ無し（`/pages/161808493`）→ `頁面 161808493`
  - 旧 `/display/{KEY}/{Title}` 形式にも対応
- モーダルの各 URL 入力の**上にラベルを表示**（入力に追従してリアルタイム更新）
- 一覧セルの tooltip も生 URL → 読めるラベルの改行リストに変更
- 入力作業はゼロ（URL を貼るだけでラベル生成）。手動上書きは将来拡張余地として保留

---

## v0.18.0 — 2026-05-31

**複数 Confluence リンクの表示を集約・モーダルから開けるように**

### 変更内容

- 🔗 列に複数リンクが付くとアイコンが縦積みになり見栄えが悪かったため、**1 アイコン＋件数バッジ**に集約（2 件以上なら右上に件数を表示）
- 列幅が常に一定になり、何件でも崩れない
- モーダルの各 Confluence URL 行に **↗ 開くボタン**を追加。入力した URL を別タブで開ける（http(s) のみ有効、未入力時は無効表示）
- 他タブの 🔗 列も同じ集約表示に統一（先頭 URL へ直接リンク・tooltip に全 URL）

---

## v0.17.1 — 2026-05-31

**モーダルエディタに狀態・Confluence URL を統合、親 Issue ラベルを追加**

### 変更内容

- モーダルヘッダーに**親 Issue の番号・名前**を小テキストで表示（どの Issue 配下の Mission かが一目でわかる）
- モーダル内に**狀態バッジ選択**を追加（バッジをクリックして選択、アクティブ以外は薄く表示）
- モーダル内に**Confluence URL 入力**を追加（複数 URL・＋ボタンで追加・✕で削除）
- **狀態セル・Confluence セルのクリック**でもモーダルが開くように変更（選択した箇所にフォーカス）
- 保存時は変更のあったフィールドのみ送信（未変更はネットワーク送信しない）
- 保存結果はすべてのセルに楽観的更新を同時適用

---

## v0.17.0 — 2026-05-31

**Mission進度の編集をモーダルエディタに変更**

### 変更内容

- Mission進度セル（Tab C）のダブルクリックで**モーダルエディタ**が開くように変更（旧：セル内インライン textarea）
- モーダルに Mission 番号・Mission 名をヘッダー表示
- 大きな textarea で快適に入力可能（最小 200px 高・最大 52vh）
- **Ctrl+Enter** で保存、**Esc** でキャンセル、オーバーレイ外クリックでもキャンセル
- 保存後は従来どおり楽観的更新（セル即時反映＋バー表示）を維持
- モーダル閉じてから保存処理をバックグラウンド実行（保存中でもすぐ次の編集へ移れる）

---

## v0.16.4 — 2026-05-31

**保存中インジケータ：伸びるバー＋色変化を両立**

### 変更内容

- 保存中の表示を、**左→右に満ちる「伸びるバー」を維持**しつつ、満ちながら**色を濃い青（0.48）→薄い青（0.14）へ変化**させる効果に変更（`cellFill` で width と background を同時アニメーション）
- 1 回のみ・**ループしない**（`forwards`）、所要 **約5秒**（保存実測 ~6 秒に合わせる）

---

## v0.16.3 — 2026-05-31

**保存中インジケータを「濃い→薄い」フェードに変更・所要時間に合わせて延長**

### 背景

保存の実測が約 6 秒で、v0.16.2 の塗りつぶし（1.8 秒）は早く終わりすぎ、残り時間が無反応に見えていた。

### 変更内容

- 保存中の表示を、セル全体が**濃い青（不透明度 0.42）から徐々に薄い青（0.05）へフェード**する効果に変更（`cellFade`）
- 1 回のみ・**ループしない**（`forwards` で最終状態を保持）
- 時間を **1.8 秒 → 5 秒**（`ease-out`）に延長し、保存実測（~6 秒）に近づけて早期完了の見た目を緩和。保存が早く終われば途中で緑フラッシュへ、長引けば薄い状態で待機

---

## v0.16.2 — 2026-05-31

**保存中インジケータを「セルの塗りつぶし」効果に変更（繰り返さない）**

### 変更内容

- 保存中の表示を、セル下端を流れる**繰り返しバー**から、**セルが左から右へ色（薄い青）で満たされていく塗りつぶし効果**に変更（`.cell-saving::before` の幅を 0→100% にアニメーション）
- アニメーションは **1 回のみ**（`animation-iteration-count: 1` 相当・`forwards` で 100% を保持）。ループしない
- 塗りは半透明（`rgba(26,115,232,0.20)`）でセル内のテキスト／バッジは読めるまま。`cell-saving` の即時背景塗り（`#E8F0FE`）は撤去し、塗りつぶしアニメーションに一本化

---

## v0.16.1 — 2026-05-31

**保存の体感改善（楽観的更新）＋砂時計を進捗バーに置換**

### 背景

Mission進度などの保存が遅く感じる根本要因は Google Apps Script Web App のサーバ往復（リダイレクト＋シート書込で 1〜3 秒）で、フロントからは短縮しきれない。そこで**体感**を改善する。

### 変更内容

1. **楽観的更新（Optimistic UI）**：狀態 / Mission進度 / 更新日の編集で、**入力値を即座にセルへ反映**し、保存はバックグラウンドで継続。完了時に緑フラッシュ＆トースト、失敗時のみ元の値へロールバック。**保存中も他セルを編集可能**（待たされない）
2. **砂時計 `⏳` を廃止**：保存中はセル下端を流れる**不確定プログレスバー**（青）を表示し、「いま保存中」を親切に可視化（`.cell-saving::before` アニメーション）
3. **読込ステータスのスピナー化**：データ取得中の `⏳ 載入中…` を、回転する**ミニスピナー**＋`載入中…` に変更（`setCloudStatus(text, loading)`）

> 注：サーバ書込自体の所要時間は従来どおりだが、ユーザーは入力直後に結果が見え、待ち時間を意識しなくなる。

---

## v0.16.0 — 2026-05-31

**「一覧性」と「詳細閲覧」の役割を明確化（Mission進度タブ＝詳細／他＝一覧）**

### 変更内容

1. **Mission進度セルの 1 行省略＋ジャンプ（Mission進度タブ以外）**
   - `Issue & Mission`（Tab A）／`會議議程`（Tab B）／担当者別（Tab D/E）では、Mission進度を **1 行省略表示（ellipsis）** に。全文はツールチップで確認可
   - Mission 行のセルは **クリックで `Mission進度` タブの該当行へジャンプ**し、対象行をフラッシュ強調（`jumpToMission()`）。フィルタで隠れている場合は Tab C のフィルタを解除して表示
   - `Mission進度` タブ（Tab C）は従来どおり**全文表示＋インライン編集**（詳細閲覧・記入の場）
2. **更新（時刻）の表示分け**
   - **Mission進度タブ**：実日時（`YYYY-MM-DD HH:mm`）＋相対表現の二段表示（詳細）
   - **その他タブ**：**漢字の相対表記のみ**（`今天` / `◯天前`）。実日時はツールチップで補助（一覧性優先）。`freshnessCell` に detailed/compact モードを追加
   - 一覧タブの「更新」列幅を 104px → 64px に縮小（相対表記は短いため）
3. 補助：`show()` をプログラム呼び出し時もタブのハイライトが付くよう改善

---

## v0.15.2 — 2026-05-31

**Mission進度タブ：停滞アラートの地色を「更新」列だけに限定**

### 変更内容

- Tab C（Mission進度）で、停滞（>7日 黄・>14日 赤）の地色が**行全体**に付いていたのを、**「更新」列セルのみ**に変更
- 実装：`renderTableC` で行に付けていた `stalled-red` / `stalled-yellow`（行全体着色）を撤去。更新セルは従来どおり `upd-warn` / `upd-stale` で黄/赤の地色を保持するため、アラートは更新列だけに出る
- 他タブ（A/B/D/E）の表示は変更なし

---

## v0.15.1 — 2026-05-31

**更新セル：相対表現の復活＋編集直後に時刻が消えない補完**

### 変更内容

1. **「◯天前」を 2 行目に復活**：更新セルは「実際の日時（1 行目）＋ 相対表現 `今天` / `◯天前`（2 行目・小さめ）」の二段表示に。停滞色（黄/赤）は相対行にも反映
2. **編集直後の時刻消失を修正**：Apps Script 未再デプロイ時は応答が日付のみ（時刻なし）で返るため、更新した行だけ時刻が消えていた。`ensureEditTimestamp()` を追加し、**応答に時刻が無ければクライアント時刻で補完**（編集はたった今行われたものなので妥当）。再デプロイ後は応答の時刻をそのまま使用

---

## v0.15.0 — 2026-05-31

**更新日を「実際の日時」表示に（相対表現はツールチップへ）／時刻の取り扱い**

### 変更内容

- **実際の日時を表示**：更新セルを `今天` / `◯天前` の相対表示から、**実際の日付 `YYYY-MM-DD`** 表示に変更。時刻を持つ値は **`HH:mm` を小さく併記**。相対表現（◯天前）は**ツールチップ**に移動（停滞色 黄/赤 は従来どおり日数で判定）
- **日付パーサが時刻を解釈**：`parseDateLoose` を拡張し、`YYYY-MM-DD HH:mm[:ss]` / `M/D/YYYY HH:mm` 等の時刻付き値を読めるように
- **更新列の幅を拡張**：各タブの「更新」見出しを 56px → 104px（日時が収まるように。リサイズ可）
- **Apps Script 応答に時刻を付与**：編集時の `更新日` 応答を `yyyy-MM-dd` → `yyyy-MM-dd HH:mm` に変更（編集直後から時刻が表示される。**反映には再デプロイが必要**）

### スプレッドシートで時刻を表示・管理するには

- 値自体は Apps Script が `new Date()`（時刻込み）で書き込み済み。**列の書式を「日時」**（例 `yyyy-mm-dd hh:mm`）に設定すれば、シート表示と gviz 経由のダッシュボード表示の双方で時刻が出る
- 手動入力時は日付に時刻も付けて入力すれば、そのまま保持・表示される

---

## v0.14.0 — 2026-05-31

**更新日を自動記録に変更（手入力をなくす）**

### 変更内容

- **更新日セルを手入力不可に**：Tab C の「更新」セルのクリック編集を撤去。`freshnessCell` に `auto` モードを追加し、ツールチップに「編輯時自動記錄（無需手動輸入）」を表示
- **編集時に自動でその日を記録**：`狀態` / `Mission進度` / `Confluence 連結` のいずれかをダッシュボードから編集すると、Apps Script 側でその行の `更新日` が自動で当日にセットされる（`狀態`・`Mission進度` は従来から自動。今回 **`Confluence URL` 編集も自動更新対象に追加**）
  - Apps Script：`Confluence URL` 書き込み時に `touchedContent = true` を立て、`更新日` を自動更新（**この挙動の反映には Apps Script の再デプロイが必要**）

> 注：本ツール（ダッシュボード）からの編集はすべて `更新日` が自動更新されます。スプレッドシートを直接編集した場合の自動スタンプが必要なら、別途 installable `onEdit` トリガーの追加で対応可能です。

---

## v0.13.3 — 2026-05-31

**Confluence アイコンを内蔵 SVG 化（ヘッダー確実表示・画像点滅の解消）**

### 問題

`benessetw.atlassian.net/favicon.ico` が読み込めない環境で、
- ヘッダーは `onerror` フォールバックにより `🔗` 絵文字のままだった
- セルは `faviconFor(url)` 失敗 → `onerror` で同じ壊れた favicon に差し替え → 再失敗 → … の**無限ループ**になり、画像が点滅していた

### 修正

- **`CONF_FAVICON` を外部依存なしの data-URI SVG（Confluence ロゴ風）に変更**。ネットワーク不要で必ず描画される
- **ヘッダー**：`<img>`（外部 URL）→ `<span class="conf-th-icon">` ＋ CSS 背景（data-URI SVG）に変更。確実に Confluence アイコンを表示
- **セル**：`faviconFor(url)`（貼り付けた URL の実 favicon）を試し、失敗時は内蔵 SVG にフォールバック。`onerror` に `this.onerror=null` を付与し、**フォールバックを一度きりに**して無限ループ（点滅）を防止

> 注：セルは貼り付けた URL の実 favicon を優先表示し、読めない場合のみ内蔵 SVG にフォールバックします。実サイトの favicon を確実に出したい場合は、その Confluence サイトの URL を貼れば自動で反映されます。

---

## v0.13.2 — 2026-05-31

**リンク列ヘッダーを Confluence favicon に／Tab C セルの継承リンク参照を撤去**

### 変更内容

- **ヘッダー行に Confluence favicon**：B/C/D/E タブのリンク列見出しを `🔗` 絵文字から **Confluence favicon の画像**に変更（読み込み失敗時は `🔗` にフォールバック）
- **セル内の衝突を解消**：Tab C の 🔗 セルから「親 Issue から継承したページ」への参照リンク（v0.13.1 の薄い参照アイコン）を**撤去**。セルは **Mission 自身の URL のみ**を表示し、未設定なら `＋` ヒントのみ
  - 親 Issue ページの継承表示は引き続き A/B/D/E タブが担う（Tab C は Mission 自身の管理に専念）

---

## v0.13.1 — 2026-05-31

**Confluence URL の「親継承」と「Mission 自身の編集」の衝突を修正**

### 問題

v0.13.0 で Tab C の 🔗 セルを貼り付け編集可能にしたが、Mission 読み込み時に Confluence URL が**親 Issue から継承**される旧仕様（`get('Confluence URL') || parentRow['Confluence URL']`）が残っていたため、Mission 自身に URL が無くても親のページが表示され、編集を開くと親 URL が初期値に入り、保存すると親 URL が Mission 行へコピーされてしまっていた。

### 修正

- Mission に **`Confluence URL自身`**（継承しない生値）フィールドを追加。Tab C の編集対象をこちらに限定
- 表示用の **`Confluence URL`**（自身が空なら親から継承する実効値）は従来どおり維持し、A/B/D/E タブの読み取り表示は不変
- Tab C：Mission 自身の URL が無く親から継承している場合は、**薄いグレーの参照リンク**として併記（クリックで親ページを開けるが、編集対象ではないことを視覚的に区別）
- 保存後は実効 URL を再計算（自身を空に戻すと親継承へ自動復帰）

---

## v0.13.0 — 2026-05-31

**Mission進度タブ（Tab C）：Confluence 連結の貼り付け編集・favicon 自動化・レイアウト調整**

### 変更内容

1. **favicon を URL ドメインから自動導出**：`faviconFor(url)` を追加し、貼り付けた Confluence サイトの favicon（`https://<host>/favicon.ico`）を自動表示。解析不可時は既定（`benessetw.atlassian.net`）にフォールバック。**利用者の追加入力は不要**
2. **Confluence URL の貼り付け編集（複数 URL 対応）**：Tab C の 🔗 セルをクリックすると編集 UI が開き、URL を貼り付け／「＋ 新增連結」で複数追加／「✕」で削除可能。複数 URL は**改行区切り**で 1 セルに保存し、各 URL を favicon リンクで表示
   - フロント：`confEditCell()` / `startConfEdit()` を追加。`applyLocalEdit` に `Confluence URL` を追加。favicon リンクのクリックは編集を発火させないガードを追加
   - Apps Script（`setup-sheet.gs`）：`WRITE_FIELDS` に `Confluence URL` を追加し、`doPost` で書き込み対応（リンク編集は進捗ではないため `更新日` は自動更新しない）
   - **⚠ 保存を有効化するには Apps Script の再デプロイが必要**（既存デプロイを「新しいバージョン」で更新し、同じ URL を維持）
3. **ヘッダー行を中央ぞろえ**：`#panel-C .simple-table thead th { text-align: center; }`
4. **Mission進度 列を既定で最大化**：Mission に固定幅、`Mission進度` を `width:100%` の貪欲列にして、初期表示でいちばん広く表示

---

## v0.12.0 — 2026-05-31

**Mission進度タブ（Tab C）を「記入しやすさ」優先に再設計**

### 変更内容

1. **列順変更**：編集対象（`Mission進度` / `狀態` / `更新`）を `Mission` の直後にまとめ、継承表示の `處` / `組` / `擔當` を 🔗 リンクの後ろ（末尾）へ移動
   - 新列順：`親 Issue / 編號 / Mission / Mission進度 / 狀態 / 更新 / 🔗 / 處 / 組 / 擔當`
   - Tab C 専用の行レンダラ `missionEditRow()` を新設（共有 `simpleRow` は B/D/E 用にそのまま維持）
2. **説明バナー削除**：「💡 狀態 セルをクリック…」の黄色い案内バナーを丸ごと削除（黄色スペースも除去）
3. **編集ヒントを繁体字化**：`雙擊編輯`（Mission進度）／`點擊變更`（狀態）／`點擊編輯日期`（更新）
4. **編集強調を薄いブルーに**：編集可能セルの hover 強調を黄色（`#FFF9DB`/`#F9AB00`）→ 薄いブルー（`#E8F0FE`/`var(--g-blue)`）に変更（入力欄は従来どおりブルー系で統一）

---

## v0.11.1 — 2026-05-31

**列幅の localStorage 保存・復元＋リセットボタン**

### 変更内容

- ドラッグ調整した列幅を **localStorage に保存**（キー：`jig:colw:<panelId>`、列インデックス→px のマップ）。再訪・リロード時に自動復元
- 各タブの操作バー（`.ctrl-bar`）に **「↔ 重置欄寬」リセットボタン**を動的に追加。押すと当該タブの保存幅を削除し、見出しの幅指定をクリアして既定レイアウトに戻す
- 保存はドラッグ完了時（`mouseup`/`touchend`）に実行。`localStorage` 不可環境でも例外を握りつぶして通常動作を維持

---

## v0.11.0 — 2026-05-31

**列幅のドラッグ調整機能を追加（DASHBOARD 以外の全タブ）**

### 変更内容

- `Issue & Mission`（Tab A）／`Mission進度`（Tab C）／`會議議程`（Tab B）／担当者別（Tab D/E）の各テーブルで、**列見出しの右端をドラッグして列幅を調整**できるようにした
- 対象テーブルに `resizable` クラスを付与し、`thead th` の右端にドラッグ用グリップ（`.col-resizer`）を起動時に 1 回付与（thead は静的 HTML で tbody のみ再描画されるため再付与不要）
- マウス／タッチ両対応。最小幅 28px。`DASHBOARD`（Tab F）は対象外

---

## v0.10.4 — 2026-05-31

**會議議程タブ（Tab B）の見出し統一・説明文削除**

### 変更内容

- `會議議程` タブの備考列見出しを `備註` → **`Mission進度`** に統一（内容は既に `simpleRow` 経由で `Mission進度` を表示済み）
- パネル冒頭の説明文「依確認會議分組顯示 Mission，依星期與時間排序的會議週次議程」を削除（見れば分かる冗長な説明のため）

---

## v0.10.3 — 2026-05-31

**Issue & Mission タブの備考列を `Mission進度` に統一し、Mission コメントを表示**

### 変更内容

- `Issue & Mission` タブ（Tab A）のテーブル見出しを `備註` → **`Mission進度`** に改名（他タブと表記を統一）
- **不具合修正**：Tab A の `renderRow` は備考セルに `事務局備註` 列のみを参照していたため、**Mission 行の `Mission進度` コメントが表示されず空欄**になっていた。`r['Mission進度'] || r['事務局備註']` のフォールバックに変更し、Issue 行は `事務局備註`、Mission 行は `Mission進度` を表示するよう修正（`simpleRow` と同じ方式に統一）
- Tab A の検索対象テキストにも `Mission進度` を追加（Mission コメント本文で検索可能に）

---

## v0.10.2 — 2026-05-31

**Mission進度タブ・インライン編集の完全動作化 ＋ シート列名確定**

### 変更内容

#### タブ名・色・配置
- `停滞警報` タブを **`Mission進度`** に改名
- タブ色を赤（`#EA4335`）に変更
- タブ順序変更：`Issue & Mission` → `Mission進度` → `會議議程` → `DASHBOARD`
- `panel-C` の border-top も赤に合わせ変更

#### Mission一覽 列名確定（`Mission進度`）
- シートの備考列名を `備註` / `事務局備註` から **`Mission進度`**（H列）に統一
- `index.html` の `OPTIONAL_COLS` に `'Mission進度'` を追加
- ダッシュボード側の `simpleRow` / `applyLocalEdit` / トースト文言もすべて `Mission進度` に更新
- `WRITE_FIELDS` のマッピング：`'備註': 'Mission進度'`

#### Apps Script (`setup-sheet.gs`) 改修
- `SHEET_ID` 定数と `_getSpreadsheet()` ヘルパーを追加（スタンドアロン Web App でも `openById()` で確実にシートを取得）
- `setupJIG` を非破壊的に改修：既存シートはヘッダ名の rename のみ実施し、データ削除・列追加はしない
- `RENAME_MAP` で旧列名 → 新列名の自動変換（`事務局備註` / `備註` → `Mission進度`、`Task` / `Issue` → `Mission`）
- `deleteColumns` コードを削除（誤ってデータ列 I / J を削除するリスクを排除）
- `getUi().alert()` をすべて `console.log` に置き換え（スタンドアロンコンテキストでハングするため）
- ステータス・更新日の書式設定をヘッダ名検索ベースに変更（列番号ハードコードをなくす）
- `diagnoseMissionSheet()` 関数追加：全ヘッダ＋サンプル行を console に出力し、実際のシート構造を診断可能に
- `fixMissionHeaders()` 関数追加：10列の正確なヘッダを強制設定（A=編號, B=Mission, C=親編號, D=管理頁面, E=戰略負責人, F=擔當, G=狀態, H=Mission進度, I=更新日, J=Confluence URL）
- `doGet()` に診断情報を追加：`WRITE_FIELDS` / `sheetName` / `headers` / `remarkColIndex` をレスポンスに含め、デプロイ確認が容易に
- `doPost()` にデバッグログを追加

#### SHEET_WRITE_URL 更新
- `index.html` の `SHEET_WRITE_URL` を最新デプロイ（バージョン14）の URL に更新
- これにより `changes: {}` のまま `ok: true` が返り続けていた問題が解消

### 不具合診断の記録（次セッション参考）
- **`changes: {}`の原因**：`SHEET_WRITE_URL` が旧デプロイ（バージョン3）を指していた。新コードはデプロイされていたが別 URL で、旧コードが呼ばれ続けていた。
- **対策**：既存デプロイに「新バージョン」を割り当てて URL を維持するか、新デプロイ URL を `SHEET_WRITE_URL` に反映する。
- **`getActiveSpreadsheet()` が null**：スタンドアロン Web App では使えない。`openById(SHEET_ID)` が必須。

---

## v0.10.0 — 2026-05-30

**停滯警報タブにインライン編集（書き込み）機能を追加**

ダッシュボード上で **狀態 / 備註 / 更新日** を直接編集して Sheet に即反映できる。会議中のクイック報告で「Sheet を別タブで開いて編集」する手間をゼロに。

### 仕組み
- `scripts/setup-sheet.gs` に **`doPost(e)`** 編集 API を追加（Apps Script Web App としてデプロイ）
- ダッシュボードから `fetch POST` で `{mission, 狀態?, 備註?, 更新日?, token}` を送信
- Apps Script が `Mission一覽` シートの該当行を更新
- 狀態 / 備註 が変わった時は **更新日 を自動 today にセット**（手動指定があればそれを優先）
- CORS 回避のため `Content-Type: text/plain`（中身は JSON 文字列）

### UI（停滯警報タブのみ）
- **狀態セルをクリック** → 5 段階ドロップダウン → 選択で即保存
- **備註セルをダブルクリック** → textarea → Enter/blur で保存（Escape でキャンセル）
- **更新セルをクリック** → date input → 変更で保存
- 編集対象セルは hover で薄黄背景＋✎ アイコンが出る
- 保存中：水色背景＋⏳
- 成功：薄緑にフラッシュ＋緑トースト
- 失敗：赤トーストで原因表示

### 安全性
- トークン（`WRITE_TOKEN`）が一致しないと書き込み拒否
- 狀態は `ALLOWED_STATUS` 5 値のみ受理
- 更新日のフォーマット異常は 400 で拒否
- 編集対象は `Mission一覽` シートのみ

### 注意点
- `WRITE_TOKEN` と Web App URL は社内のみで使う前提（公開静的サイト上にあるので URL を知る人は誰でも編集可能）
- Sheet の編集履歴は Apps Script 実行者名義になる → 「誰が編集したか」は記録されない（v1 の制約）
- 必要なら将来 localStorage に編集者名を保存して別シートに log する拡張可能

### 事務局の作業（Apps Script デプロイ）
1. Apps Script エディタを開く → `scripts/setup-sheet.gs` の中身を貼り付け（更新分）
2. 保存 → 右上「デプロイ」→「新しいデプロイ」
3. 種類：**ウェブアプリ**
4. 実行ユーザー：**自分** ／ アクセス：**全員**
5. 「デプロイ」→ 権限承認 → **Web App URL をコピー**
6. URL を私に渡す → `index.html` の `SHEET_WRITE_URL` に埋め込みコミット

---

## v0.9.12 — 2026-05-30

**全タブの Mission に Confluence リンク・結案 表示トグル**

### 1. Confluence リンクを全タブの Mission 行に追加
- B / C / D / E：thead に「🔗」列（34px）を末尾に追加、Mission 行右端に Confluence favicon リンク
- 親 Issue から継承された `Confluence URL` を使う（A タブと同じ仕組み）
- DASHBOARD の停滯／最近活躍リストにも Confluence アイコンを追加（右端 26px 列）
- URL が空の Mission は何も表示しない（リンクなしの空セル）

### 2. 「顯示結案」トグル（全タブ連動・既定 OFF）
- A/B/C/D/E の各 ctrl-bar に **`☐ 顯示結案`** チェックボックスを追加
- 既定 OFF：`狀態 = 結案`（旧「完成」も同義）の Mission は全タブで非表示
- 1 つのチェックを変更すると全タブのチェックが同期、全タブが再描画
- 内部状態 `_showClosed` ＋ `getShowClosed()` / `setShowClosed()` / `wireClosedToggles()` で集中管理
- A タブの Issue 行が結案の場合も連動して非表示

DASHBOARD は集計性質上、結案も含めた全体状況を表示（トグル対象外）。

---

## v0.9.11 — 2026-05-30

**B/C/D/E 行密度・C 並び順・DASHBOARD 全面再設計**

### 1. 行密度（B/C/D/E）
`.simple-table` の td padding 5px → 2px、font 11px → 10px、line-height 1.3 に変更。
A の Mission 行と同じ高さ（約 60% コンパクト）に統一。各種バッジサイズはそのまま。

### 2. 停滯警報の並び順
更新日順 → **Sheet 自然順**（A タブと同じ：親 Issue の Sheet 順 → Mission 自身の Sheet 順）に変更。
停滯度は行背景色（紅／黃）で表現済みなので、並びは「Issue ごとにまとまる」読みやすい順に。

### 3. タブ名：儀表板 → **DASHBOARD**

### 4. DASHBOARD 全面再設計

撤去：擔當別 Mission 件數 / 部門別 Mission 件數 / 親 Issue 別 Mission 件數（3 つの bar chart）

代わりに：

**(1) Mission Pipeline — 横スタックバー**
全 Mission の進度分布を 5 段階のカラフルなセグメントで視覚化。各セグメントの幅が件数に比例。それぞれに件数・段階名・割合を表示。

**(2) 健康指標 KPI 5 枚**
- 總數
- 活躍率（進行中＋結案 ÷ 總數）
- 結案率
- 停滯率（>14天 ÷ 總數）
- 擔當未割当数

**(3) 須關注 — 停滯 Top 10**
更新日が古い順に 10 件、リスト形式で表示。各行に Mission 名（親 Issue 名付き）／擔當／狀態バッジ／N 天前。>14 日は赤背景、>7 日は黃背景。

**(4) 最近活躍 — Top 10**
直近 7 天で更新された Mission を新しい順に 10 件。

「総量集計」→「ピックアップ／健康指標」中心の構成に。会議で「どこを掘るか」「何が動いてるか」を即座に判別できる。

---

## v0.9.10 — 2026-05-30

**B/C/D/E に「組」列・「組」セレクタを追加**

これまで 處 だけで絞っていたが、組まで絞れるように：

- **列**：thead に `組` を 處 の右側に挿入（4 タブとも）。`simpleRow` に
  `opts.showGroup` 追加で 處 セルの直後に組セルを描画。
- **セレクタ**：ctrl-bar に `組：全部` を追加（4 タブとも）。
- **處 → 組 カスケード**：處 を選ぶと組のリストはその傘下のみに自動絞り込み。
  共通ヘルパ `populateDeptGroupOwner(panelKey, missionFilter)` に集約し、
  B/C/D/E すべてで再利用。
- D/E では「自分の擔當 or 戰略負責人」の Mission のみが候補に出るよう、
  populateDeptGroupOwner に `missionFilter` 引数を持たせて絞り込む。

これで「経營戰略 × 1.經營戰略 組の進行中 Mission」のような細かい
切り口で即座にピックアップできる。

---

## v0.9.9 — 2026-05-30

**親 Issue 列を 1 行表示に制限**

B / C / D / E の親 Issue セルが長い Issue 名で 2 行になっていたのを、常に **1 行表示**に：

- `.parent-cell` に `white-space: nowrap; overflow: hidden`
- `.pc-name` を `display: inline-block` ＋ `max-width: 100px` ＋ `text-overflow: ellipsis` で末尾 `…` 切り
- セル全体に `title="戰略-1 BCL註冊用戶擴大"` を付与 → **ホバーで全文ツールチップ表示**

短い Issue 名はそのまま、長い名前のみ末尾省略になり、行の高さが安定。

---

## v0.9.8 — 2026-05-30

**B/D/E にフィルタ追加・會議議程の列ズレ修正・列幅整理**

### 修正：會議議程の thead 列順
B タブの thead は `... 處 / 擔當 / 狀態 / 備註 / 更新` だったが、`simpleRow` の描画順は `... 處 / 擔當 / 備註 / 狀態 / 更新`。**狀態と備註が逆**だったため、狀態バッジが備註列の位置に表示されていた。thead 側を simpleRow の順に揃えた。

### 列幅とテキスト折返し
- B：親 Issue 170→160 / 編號 90→88 / 處 80→76 / 擔當 70→76 / 狀態 74→80
- CSS：`.simple-table td.dept/.num/.st/.upd` に `white-space: nowrap` を追加、`.memo` は `normal`（備註は適度に折返し可）

### 會議議程（B）にフィルタ追加
C と同じ 4 セレクタ＋清除＋カウント：
- 處 / 擔當 / 狀態 / 更新（>14d / 8-14d / ≤7d / 未設定）
- フィルタ後に空になった会議グループは非表示

### 詩雅（D）／育菱（E）にもフィルタ追加
- 處（その人物の関連 Mission に出てくる 處 のみリスト化）
- **角色**：全部 / 擔當 / 戰略負責人 / 兩者（このパネル特有）
- 狀態 / 更新
- カウント表示は「顯示 N / M 筆」＋（フィルタ後の）角色内訳

### 共通化
ステータス / 鮮度のマッチング判定を `matchStatus` / `matchStale` ヘルパに抽出し、B/C/D/E で共有。

---

## v0.9.7 — 2026-05-30

**詩雅・育菱パネルは 戰略負責人 マッチも表示**

それまでは `擔當 = 詩雅` の Mission のみだったが、`戰略負責人 = 詩雅` の Mission（親 Issue を持ってる側）も同じ画面に統合表示する。

### 列追加：角色
8 列目に **角色** カラムを追加し、各 Mission がどの役割で含まれているかをバッジで表示：
- **擔當**（橘）：自分が直接の実行担当
- **負責人**（紫）：自分が親 Issue の戰略負責人で、間接的に関与
- 両方該当する Mission には**両方のバッジ**を併記

セクション見出しに件数内訳を表示：
> ▶ 詩雅 相關 Mission · 合計 8 筆（擔當 5 / 戰略負責人 2 / 兩者 1）

実装：
- `simpleRow` に `opts.extraCellAfterDept` を追加（汎用的に「處」セルの直後に任意のセルを差し込めるように）
- `renderPersonView` のフィルタ条件を `擔當 OR 戰略負責人` に拡張
- 各行の役割を `isOwner` / `isStrategic` フラグで判定し、角色セル HTML を組み立てて simpleRow に渡す

これで「自分が握っている全 Mission」が 1 画面で見える。1on1 時の議題出しが網羅的に。

---

## v0.9.6 — 2026-05-30

**停滯警報にフィルタ追加 + 説明文の繁體統一**

C タブ（停滯警報）の上部に 4 つのセレクタを追加：
- **處**：全部 / 部門名
- **擔當**：全部 / 各擔當者
- **狀態**：全部 / 未開始 / 策劃中 / 需確認 / 進行中 / 結案（結案フィルタは旧「完成」も拾う）
- **更新**：全部 / >14 天無更新 / 8-14 天無更新 / ≤7 天 / 未設定

複数フィルタは AND 条件。右側に `顯示 N / M 筆` のカウント表示、`清除` ボタンで一括リセット。

### 説明文の日本語残りを繁體中文に統一
- B：「確認會議**ごとに** Mission **を曜日・時間順に並べた**」→「依確認會議分組顯示 Mission，依星期與時間排序」
- C：「更新日**が古い順に並べた** Mission 停滯**リスト**（>14 **日** 赤）」→「依更新日由舊至新排序的 Mission 停滯清單（>14 天 紅）」
- D：「詩雅**が**擔當**として割り当てられている** Mission」→「詩雅 擔當的 Mission 清單」
- E：同上（育菱）
- 詩雅・育菱ボタンの title 属性：「○○の Mission を表示」→「顯示○○擔當的 Mission」

---

## v0.9.5 — 2026-05-30

**Mission 狀態を 3 段階 → 5 段階に拡張**

| 狀態 | バッジ色 | Sheet bg |
|---|---|---|
| 未開始 | 灰 `#9AA0A6` | `#EEEAE0` |
| 策劃中 | 青綠 `#00ACC1` | `#D1F2F7` |
| 需確認 | 橘 `#F9AB00`（濃灰文字） | `#FEE7BB` |
| 進行中 | 藍 `#4285F4` | `#DCEBFB` |
| 結案 | 綠 `#34A853` | `#D4F2DD` |

進捗の細やかさを上げ、「需確認」段階で関係者の確認待ちが明示的に見える運用に。

更新箇所：
- `index.html`：CSS の `.st-策劃中/.st-需確認/.st-結案` を追加、`STATUS_VALUES` Set 拡張、`renderTabF` の集計を 5 区分に、儀表板カードを 6 種類に増やす（總數＋5 段階＋擔當未割当）
- `scripts/setup-sheet.gs`：`STATUS_VALUES` / `STATUS_COLORS` 更新、データ検証メッセージも 5 段階表記
- `scripts/build-template-xlsx.py`：同上

旧ラベル「完成」も「結案」と同義扱いで集計（移行済みデータに完成が残っていても OK）。

---

## v0.9.4 — 2026-05-30

**タブ整理・人物ボタン化・儀表板を Mission 専用に**

1. **タブの記号 A:〜F: を削除**：純粋なタブ名のみに
   - `A：Issue & Mission` → **`Issue & Mission`**
   - `B：會議議程` → **`會議議程`**
   - `C：停滯警報` → **`停滯警報`**
   - `F：儀表板` → **`儀表板`**

2. **タブ行は 4 つに削減**。詩雅・育菱は右肩 ⚙ 管理者專用ボタンの下に**横並び**の小ボタンに退避：
   - `👤 詩雅` / `👤 育菱` クリックで該当 panel に切替
   - 各人ボタンに active 時の色を持たせる（詩雅=紅 tint、育菱=紫 tint）
   - panel-D / panel-E の内容は無変更、起動経路だけ変更
   - 4 タブの 4 番目（儀表板）は青綠 #00ACC1

3. **儀表板を Mission 専用に**：
   - 旧「Issue 概況」「部門別 Issue 件數」「戰略負責人別 Issue 件數」セクションを撤去
   - 残る：Mission 概況／停滯／擔當別 Mission 件數／部門別 Mission 件數／**親 Issue 別 Mission 件數**（新規・どの Issue にどれだけ Mission がぶら下がっているか）

タブ行が常に「業務本流の 4 つ」だけになり、個人ビュー（D/E）と管理者 console は右肩に集約 → 主要画面に集中できる構成へ。

---

## v0.9.3 — 2026-05-30

**B/C/D/E に「親 Issue」列、タブ名を繁體中文に統一**

各 Mission 行の左端に **親 Issue 列**を追加：
- 灰色バッジに親 Issue 編號（例：`戰略-1`）
- 続けて Issue 本文（例：`BCL註冊用戶擴大`）
- **連続する同じ親 Issue では最上行のみ表示**、以降は空白セル（背景わずかに灰）
- 親と Mission の関係が一目で読める

dedup を効かせるため、各タブ内で **親編號の昇順で副ソート**：
- B：會議グループ内で親編號 ASC
- C：更新日が同じグループ内で親編號 ASC
- D/E：全件を親編號 ASC

タブ名の日本語混在を解消（純繁體中文に）：
- `B：會議アジェンダ` → **`B：會議議程`**
- `C：停滞警報` → **`C：停滯警報`**（滞 → 滯）
- `F：Dashboard` → **`F：儀表板`**

A / D / E は元から繁體 + 固有名詞のため変更なし。

---

## v0.9.2 — 2026-05-30

**B / C / D / E を Mission 専用ビューに**

A 以外のタブは実行ユニットである **Mission** にスコープを限定。Issue は A の階層表示と Dashboard の概況数字でカバー。

| Tab | スコープ | 並べ方 |
|---|---|---|
| B：會議アジェンダ | Mission のみ | 親 Issue の `確認會議` で集約・曜日順 |
| C：停滞警報 | Mission のみ | `更新日` 古い順、>14d 赤・>7d 黃 |
| D：詩雅 | `擔當 = 詩雅` の Mission のみ | 投入順 |
| E：育菱 | `擔當 = 育菱` の Mission のみ | 投入順 |

- 簡素化：C の「種別」列を撤去（全行 Mission なので不要）
- 「負責人」列 → **「擔當」列**（B/C で表示。Mission 行は Mission 自身の擔當を見る）
- ヘッダ説明文を Mission 限定の旨に更新
- D/E は Issue セクションを廃止し、Mission 単一セクションのみに（詩雅・育菱が 擔當 を持つもののみ表示）

Issue の 戰略負責人 / 確認會議 等は引き続き A で完全に見られるため、情報損失はゼロ。

---

## v0.9.1 — 2026-05-30

**タブ B 〜 F を全面再編**

A 以外のタブは元の機能を A の検索＋フィルタが事実上吸収していたため、別の切り口へ全面入れ替え。

| Tab | 役割 |
|---|---|
| A | Issue & Mission（変更なし） |
| **B：會議アジェンダ** | `確認會議` ごとに Issue を曜日順に並べた週次アジェンダ |
| **C：停滞警報** | 全 Issue / Mission を更新日が古い順に並べる。>14日=赤、>7日=黄 |
| **D：詩雅** | 詩雅が `戰略負責人` または `擔當` の項目を一覧 |
| **E：育菱** | 育菱が `戰略負責人` または `擔當` の項目を一覧 |
| **F：Dashboard** | 概況サマリ：Issue/Mission 件数、停滞件数、部門別・担当別バー |

旧 B（會議-頁面-Issue 表）・旧 C（部門別）・旧 D（行事曆）は廃止。旧 D の曜日視点は新 B が吸収。

**Admin（資料來源 console）はタブ行から退避し、右上 ⚙ 管理者專用 ボタン経由のみアクセス可能に**。show('Admin') で開く。タブ行が常に主要 6 タブだけになり、社員に admin の存在が一切露出しなくなる。

- 新 CSS：`.simple-table`（B/C/D/E 共通）、`.dash-*`（Dashboard 用カード／バー）、6 番目タブ色 #00ACC1（teal）、`#panel-Admin` 灰アクセント
- 新 JS：`simpleRow()`（B/C/D/E 共通行レンダラ）、`renderTableB/C`（書き換え）、`renderPersonView()`（D/E 共用）、`renderTabF()`（Dashboard 統計）

---

## v0.9.0 — 2026-05-30

**タブの名称と縮小**

- タブ A の表示名を `A：Issue管理主檔` → **`A：Issue & Mission`** に変更
- タブ全体を約 **75%** に縮小：
  - padding：10px/24px → **7px/18px**
  - font-size：13px → **10px**
  - border-radius：8px → **6px**
  - 間隔（gap）：4px → **3px**

これでコントロールバーと同じ密度で頭に並び、テーブル本体の表示領域が更に広がる。

---

## v0.8.9 — 2026-05-30

**更新日の解析を堅牢化**

Sheet 側で 更新日 列をカレンダーピッカー入力にしたところ、ダッシュボードでうまく日付が表示されない問題に対応。`parseDateLoose` を新設して以下の形式に対応：

- `YYYY-MM-DD` / `YYYY/MM/DD`（既存・Taiwan ロケール既定）
- `M/D/YYYY` / `D/M/YYYY`（Sheets ピッカーが英語/カスタムロケールで出す形式）
- `YYYY年M月D日`（中文表記）
- Excel/Sheets シリアル値（念のため）
- ブラウザ標準 `Date.parse` への fallback（`May 30, 2026` 等）

時刻部分が後ろに付いていても先頭の日付を拾えるよう anchors を緩めた。解析失敗時はセルに生の値を表示し、`title` で「解析できず」を示し、`console.warn` にも記録（DevTools で原因が見える）。

---

## v0.8.8 — 2026-05-30

**テーブルヘッダ行の視認性アップ**

- 背景：`#F8F9FA`（極淡灰）→ **`#E8EAED`**（一段濃い灰）
- 文字色：`#5F6368`（中灰）→ **`#202124`**（ほぼ黒）
- 太さ：500 → **700**

データ行とヘッダの境界がはっきりして列名が読みやすくなる。

---

## v0.8.7 — 2026-05-30

**コントロールバーを 70% に縮小**

- フォントサイズ：12px / 11px → **9px / 8px**
- 入力欄・セレクタの padding：6px/10px → **4px/7px**、高さを 24px 固定に
- ボタン padding：6px/12px → **4px/8px**
- チェックボックス：標準 → **11×11px**
- セパレータ高：18px → **13px**
- 検索欄最小幅：220px → **150px**
- 要素間 gap：8px → **6px**

縦サイズが小さくなった分、テーブル本体を見られる領域が広がる。

---

## v0.8.6 — 2026-05-30

**「全部展開/全部收合」ボタンを追加**

- 上部コントロールバーに `▶ 全部展開` ボタンを追加
- ワンクリックで全ての Mission 行を一括展開（再クリックで `▼ 全部收合` に切り替わって一括折り畳み）
- 全 Issue 行の ▶/▼ アイコンも同期して切替
- データ再読込時はボタン状態を初期（▶ 全部展開）にリセット
- フィルタの `row-hidden` レイヤとは独立して動くので、フィルタで絞った状態で展開しても正しく動作

---

## v0.8.5 — 2026-05-30

**バッジ縮小・Mission 行のスリム化・会議列のデフォルト非表示**

- **Mission 編號をバッジ化**：親 Issue の處（部門）色で塗りつぶし＋白文字の pill。`戰略-1-M1` などが小さな彩色チップに
- **狀態バッジを縮小**：Issue 用 `st-badge` も padding/font を一段小さく（11→10px、padding 3→2px、border-radius 12→10px）
- **Mission の狀態バッジは更に小さく**（9px・padding 1px/6px）
- **Mission 行は處・組セルを空欄表示**：親 Issue で既に提示されている部門情報を二重表示しない（編號バッジの色で部門が分かるようになったため文字情報は不要）
- **確認會議・會議時間 列をデフォルト非表示**：`<table class="hide-mtg">` を初期付与、チェックボックスもデフォルト未チェック。必要時のみ手動で ON

---

## v0.8.4 — 2026-05-30

**Mission 行を更に静かに / 親 Issue の部門色を継承**

- 行高：70% → **約 60%**（td padding 2→1px、line-height 1.2→1.3 で行間バランス）
- フォント：lbl/dept 等 11px → **10px**、編號は 10px → **9px**
- **Mission 文字色を親 Issue の處（部門）色に同期**：
  - 經營戰略 = 青、整合行銷 = 緑、商品事業 = 黃、CX計劃 = 紅、資訊事業 = 紫、財務 = 青綠、總管理 = 灰
  - JS 側で `mc-<color>` クラスを TR に付与、CSS は `color: inherit` でセル全体に伝播
  - これで Mission 行が「親と同じ部門所属」が一目で分かる
- **Mission の Confluence セルも白**：淡青の地色 `#F8FBFF` を解除、リンクの文字色も TR から継承（部門色になる）

これで Mission 行は「親の処色をうっすら帯びた、白くて細い枝」として親 Issue の傘下にしっかり収まる見た目に。

---

## v0.8.3 — 2026-05-30

**Mission 行を更に控えめに / 階層差をはっきり**

- 先頭マーク：**↳ → ▶**（小さめ・薄グレー）
- 位置：左 4px → **左 14px**、テキスト padding 18px → **28px**（マークも本文も右に寄せて Issue とのインデント差を強調）
- 行高：80% → **約 70%**（td padding 3→2px、line-height 1.25→1.2）
- 處（部門色分け）セル：Mission 行では **色塗りなし・白背景**。親 Issue の部門色が主張、Mission は静かに従属

これで Issue が「親の主行」、Mission が「右下にぶら下がる細い枝」という階層感が一目で見える。

---

## v0.8.2 — 2026-05-30

**Issue / Mission の階層を視認しやすく**

色の役割を反転：
- **Issue 行**：薄い地色 `#F4F7FB`（淡い blue-grey）。主行であることが一目で分かる
- **Mission 行**：白背景に変更（従来は薄グレー）。子要素として「軽く」見える

Mission 行の**高さを Issue の約 80%** に：
- セル上下 padding を 6px → 3px
- line-height を 1.4 → 1.25
- font-size はそのまま 11px（読みやすさは維持）

ホバー時の色を `#F0F7FF` → `#E3EEFD` に少し強めて、Issue 地色との差を残す。

---

## v0.8.1 — 2026-05-30

Mission の `擔當` は親 Issue から継承しないように変更。空欄なら空欄のまま表示する。

戰略負責人と違って擔當（実務担当）は親が決まっても自動的に同じ人になるとは限らないため、未割当を「未割当」のまま見せる方が運用上正確。戰略負責人は引き続き親 Issue から継承。

---

## v0.8.0 — 2026-05-30

**「擔當」列を追加（Phase A：コード先行デプロイ）**

- Tab A の列構成を 11 → 12 列に。**「擔當」**列を「組」と「戰略負責人」の間に挿入
- 上部コントロールバーに **擔當フィルタ**を追加：
  - 處 / 組 と独立したセレクタ（處・組の選択に依存せず全擔當を列挙）
  - 「自分のものにフォーカス」を 1 操作で
- 検索（searchA）の対象に擔當の値も含める
- Mission 行は親 Issue の擔當を**継承**（自分で別の擔當を設定すれば上書き）
- Mission一覽 シートにも `擔當` 列があれば読む（任意・無くてもエラーにならない）

**事務局の作業（Phase B）：**
1. Google Sheets で `戰略負責人` の左に列を挿入
2. ヘッダに `擔當` と入力
3. （Mission一覽 にも同様に追加・任意）
4. 値は徐々に埋めれば OK（空欄は ISSUE/MISSION バッジ的に「未設定」と読める）

---

## Phase 2 ツール（v0.7.3 相当・ダッシュボード本体は無変更）— 2026-05-30

**スプレッドシート移行用スクリプトを Mission 化**

- `scripts/build-template-xlsx.py`：
  - 出力シート名を `Task一覽` → **`Mission一覽`**
  - 列ヘッダ `Task` → `Mission`
  - 編號の採番を `*-T#` → **`*-M#`** に変更（例：`戰略-1-T1` → `戰略-1-M1`）
  - 入力側は `Task一覽` / `Mission一覽` どちらでも認識（互換）
  - 主檔の `種別=Task` 行と `種別=Mission` 行の両方を移行対象に
  - 既存編號が `*-M#` 形式ならそのまま保持、それ以外は再採番
- `scripts/setup-sheet.gs`：Apps Script も `Mission一覽` 命名に統一

これで事務局が xlsx を push → 私が変換 → Google Sheets に再インポートで実データが Mission 構造に揃う。

---

## v0.7.2 — 2026-05-30

**Mission 行が出ない件の調査・修正**

症状：v0.7.1 でクラス名は正しくなったが、それでも Issue 行に ▶ が出ず Mission 行が展開できない。

原因分析（仮説）：gviz は無効なシート名に対して 400 を返すこともあれば、200 OK で 1 枚目（Issue主檔）の CSV を返してしまうことがある。後者の場合：

1. `loadMissionsAndMerge` が Mission一覽 を取りに行く
2. 実際には存在しないが gviz が Issue主檔 の 35 行を返す
3. 35 行を Mission として取り込んでしまう
4. 各 Mission の `親編號` が空欄なので親に attach できず全部 orphan
5. 結果として本物の Mission は読み込めず、Issue 行にも ▶ が出ない

対策：
- Mission シート判定の必須条件として **「親編號」列が headers に存在すること**を追加。
  Issue主檔 にこの列が無ければ、Mission シート扱いせずに次の候補へスキップ。
- 各候補ごとの読込結果（HTTP ステータス／空／親編號列なし／件数／親一致／親不明）を
  console.info / console.warn と `window._missionLoadDebug` 配列に記録。
- 雲端ステータスバーは Mission が 0 のときに読込ログの要約を末尾に追記表示。
  「sheet "Mission一覽": HTTP 400 ／ sheet "Task一覽": 4 件読込 / 親一致 3 / 親不明 0」のように見える。

これで実機での確認時、ステータス文字列を見ればどこで詰まっているかが一目で分かる。

---

## v0.7.1 — 2026-05-30

**v0.7.0 のバグ修正：Mission 行のクラス名漏れ**

v0.7.0 でクラス名を `task-row` → `mission-row` に変えたが、`renderRow` 内の文字列リテラル 1 箇所だけ書き換え漏れがあり、Mission 行が描画時に旧名 `task-row hidden` のままになっていた。結果：

- CSS の `tr.mission-row.hidden { display: none }` が当たらず、本来折りたたまれているはずの Mission 行が無スタイルで普通に表示
- `toggleMissions` の `tr.mission-row[data-parent=...]` セレクタも当たらず、▶ クリックが効かない
- 区別が付かないため「Mission 行が見えない」状態に見えていた

修正：`const cls = isMission ? 'mission-row hidden' : 'issue-row'` に統一。

---

## v0.7.0 — 2026-05-30

**用語変更 Phase 1：Task → Mission（コード側のみ・後方互換あり）**

「Task」は作業的なニュアンスが強いため「Mission」に統一。
本リリースはダッシュボード側だけ先行で対応し、スプレッドシートは未移行でも動く。

### UI 表記の変更（すべて Mission に）
- 列ヘッダ：`Issue / Task` → `Issue / Mission`
- 空狀態バッジ：`TASK` → `MISSION`
- 検索プレースホルダ：`Task` → `Mission`
- 雲端ステータス：`Issue N / Task M` → `Issue N / Mission M`

### 内部実装の Mission 化
- CSS class：`.task-row` → `.mission-row`、`.st-type-task` → `.st-type-mission`
- 関数名：`toggleTasks` → `toggleMissions`、`loadTasksAndMerge` → `loadMissionsAndMerge`
- フラグ：`isTask` → `isMission`、`hasTasks` → `hasMissions`
- ツリー構造：`node.tasks` → `node.missions`

### 後方互換（Phase 2 完了まで保持）
- シート名：`Mission一覽` を**優先**、無ければ旧 `Task一覽` をフォールバック取得
- 列名：`Mission` / `Task` / `Issue` のどれでも内容列として認識
- 種別値：`'Mission'` も `'Task'` も Mission 行として判定（rowsToData で
  `'Task'` → `'Mission'` に正規化、内部判定は Mission のみ）
- 主檔（Issue主檔）の `種別` 列に `Task` 値が残っていても自動で Mission 扱い

### Phase 2 で行うこと（次回）
- `build-template-xlsx.py` を Mission 対応に拡張：
  - 旧 `Task一覽` シート → `Mission一覽` にリネーム
  - 列 `Task` → `Mission`、`種別=Task` 値 → `'Mission'`
  - 編號 `*-T#` → `*-M#`
- `scripts/setup-sheet.gs` も Mission 命名に追随
- 事務局がスプレッドシートを再インポート
- 動作確認後、Phase 3 で後方互換コードを段階撤去（数週間後で OK）

---

## v0.6.4 — 2026-05-30

頂部の 4 色帶（青/紅/黃/綠）を撤去。CSS（`.g-strip`）と HTML 双方を削除。

---

## v0.6.3 — 2026-05-30

**分頁配色の入れ替え**

- ユーザ指定により分頁の色を 3 色ローテーション：
  - A：綠 → **藍 #4285F4**
  - B：藍 → **黃 #FBBC04**
  - C：黃 → **綠 #34A853**
  - D：紅（変更なし）
  - E：紫（変更なし）
- panel 上端の 3px アクセントも同じ配色に追随。

---

## v0.6.2 — 2026-05-30

**配色をより鮮やかに / より Google らしく**

- **頁面頂部に Google 4 色帶**を追加（青/紅/黃/綠の細帯、アイコニックなアクセント）
- **タブ**を Material 淡 tint からフィルド配色に変更：
  - Tab A 綠 #34A853・Tab B 青 #4285F4・Tab C 黃 #FBBC04・Tab D 紅 #EA4335
  - active 時は白文字（C は対比のため濃灰文字）、軽い陰影＋微小な上方移動
- **panel** の上端に active タブと連動した 3px 色アクセントを追加
- **狀態バッジ**を solid fill に変更（pill 形＋白文字）：
  - 未開始=灰 #9AA0A6 / 進行中=青 #4285F4 / 完成=綠 #34A853
- **空狀態の ISSUE/TASK バッジ**も色味を強化（淡 tint 背景＋濃色文字）
- **處（部門）セルを色分け**：
  - 經營戰略=青 / 整合行銷=綠 / 商品事業=黃 / CX計劃=紅 / 資訊事業=紫 / 財務=青綠 / 總管理=灰
  - JS 側で `dept-c-*` クラスを付与、CSS で背景＋濃色文字
- **行 hover** に Google Blue の薄 tint（`#F0F7FF`）。色付き處セルは brightness で控えめに連動
- **Confluence 列**に淡い青背景アクセント（`#F8FBFF`）と favicon サイズ拡大
- **版バッジ** v0.6.2 を Google Blue pill に
- favicon サイズを 28px → 32px に

---

## v0.6.1 — 2026-05-30

**繁體中文統一 + Google 色系設計**

- ダッシュボード UI の日本語文言をすべて繁體中文に統一：
  - `Remarks` → `備註`
  - 検索プレースホルダ：`🔍 搜尋 Issue / Task / 負責人 / 組...`
  - フィルタ既定：`處：全部` / `組：全部`
  - チェックボックス：`顯示會議欄`
  - クリアボタン：`清除`
  - 表示件数：`顯示 N / M 筆`
  - 鮮度表示：`今天` / `N 天前`
  - ローディング：`正在從雲端載入資料…`
- CSS を **Google Material 色系**に刷新：
  - フォントを `Roboto + Noto Sans TC` に変更
  - 主色を Google Blue (#1A73E8) / Green (#1E8E3E) / Yellow (#F9AB00) /
    Red (#D93025) / Grey (#5F6368) に統一
  - タブの active カラー、ステータスバッジ、軸タグ、按鈕、Confluence リンク、
    更新日鮮度色、ctrl-bar、ver バッジなどを Material tint へ
  - 罫線・境界線を `#DADCE0`、ヘッダ背景を `#F8F9FA`（Google Surface）
  - 角丸（panel/button）を 4–8px に整理、影を控えめに
  - ステータスバッジを pill 形（border-radius:12px）に
  - フォーカス時のアウトラインを Google Blue リングに

---

## v0.6.0 — 2026-05-30

**Tab A の UX 改善（検索・フィルタ・列折り畳み・Confluence 列統合）**

- `管理頁面` 列と末尾の `📄` 列を撤去し、その位置に **Confluence 列**を統合：
  Confluence の favicon ＋ 管理頁面名（クリックで Confluence へ）。
- `確認會議` `會議時間` の 2 列を **折り畳み可能**に。上部チェックボックスで一括 show/hide。
- `事務局備註` → **Remarks** に改名（ヘッダのみ）。
- 空欄の `狀態` は「-」をやめ、**行種別バッジ**（`ISSUE` / `TASK`、薄グレー破線枠）に。
  「欠落」ではなく「未評価」と一目でわかる。
- **處 / 組のセレクタ**を上部に追加。組は處に連動して絞り込まれる。
- **ページ内検索**を追加：編號 / Issue / 處 / 組 / 戰略負責人 / 管理頁面 / 確認會議 / Remarks に対して
  大文字小文字無視で部分一致。Task 行は親 Issue が表示中のときのみ可視。
- フィルタ右側に「N / M 件表示」のカウント表示、クリアボタンで全リセット。
- ページ左上のロゴに **favicon.svg** を表示（タブのアイコンと同じもの）。
- 静的フォールバック（古い HTML テーブル 50 行）は loading 行に置換。
  雲端読込が標準動作であり、フォールバック行は構造ズレの温床になっていたため。

---

## v0.5.1 — 2026-05-30

**Task シート連携の Unicode 修正 + xlsx マイグレーション**

- 修正：Task 一覽シート名の Unicode 不一致を修正。
  v0.5.0 は `Task一覧`（日本語「覧」, U+89A7）を見ていたが、実際の
  シートは繁體中文の `Task一覽`（U+89BD）。一字違いで連携が空振りしていた。
  index.html / docs / Apps Script / xlsx ビルダ をまとめて `覽` に統一。
- 改善：`scripts/build-template-xlsx.py` を強化：
  - 既存の Issue主檔 内に混在している `種別=Task` 行を抽出し、
    Task一覽 シートに自動移行（編號は `親編號-T1, -T2, ...` で採番）
  - 移行後の Issue主檔 から `種別` `親編號` 列を撤去
  - 旧 Task 行の Confluence URL は古い行を指す数式だったため破棄
    （Task のリンクはダッシュボード側で親 Issue から継承）

## v0.5.0 — 2026-05-30

**Tab A スリム化 + Task 一覧シート対応**

- 表示列を 14 → **12 列** に削減：`區分` / `軸` をダッシュボードから撤去（シート側のデータは保持）。
- セクション見出し（共通／個別）も完全廃止。グループ概念に縛られないフラット表示に。
- **Task は別シート（`Task一覽`）で 1 枚に集約管理**する設計に変更：
  - `Issue主檔` から `種別` `親編號` 列を撤去（事務局の入力負荷を軽減）
  - 新シート `Task一覽` を読み込み、`親編號` で Issue にネスト → ▶ 展開で表示
  - 親 Issue から `處/組/管理頁面/確認會議/會議時間` を自動継承（Task シートに書かなくてよい）
  - Task シートが存在しなくてもエラーにならず、Issue だけで動作
- 雲端ステータスに Issue 件数と Task 件数を分けて表示。
- `docs/sheet-setup.md` を全面書き換え（v0.5.0 の 2 タブ運用に対応）。

---

## v0.4.0 — 2026-05-30

**Tab A の列ずれを根本対策**

- 修正：`區分` セルの `rowspan` を全廃し、毎行 1 セル emit に変更。
  これにより「Issue 1 行 + Task 複数行（display:none）」と `rowspan` が
  噛み合ってブラウザの列計算がズレる問題を根本的に避ける。
- 視覚的には連続行を CSS（`.grp-cont`：上罫線透明＋テキスト透明）で
  マージして見せるため、見た目は従来の rowspan 版とほぼ同じ。
- 全行を共通の `renderRow()` ヘルパーで生成。Issue 行・Task 行のセル数を
  恒等的に 14 セルに統一し、列ずれが発生しないことを構造的に保証。

---

## v0.3.0 — 2026-05-30

**バージョン管理 開始**

- 追加：UI 右上に版號バッジ表示（マウスオーバーで build 日付・備考を tooltip 表示）
- 追加：本ファイル（CHANGELOG.md）でバージョン履歴を管理
- 運用：今後の変更ごとに `APP_VERSION` を上げ、本ファイルに追記する

---

## v0.2.1 — 2026-05-30

**Tab A 新列の空セル整理**

- 修正：`事務局備註` 空セルの `—` を消し、本当に空のときは何も表示しない
- 修正：`更新` 空セルの `—` も同上
- 微調整：`狀態` 空状態の `—` を薄いグレー＋細字に弱める（未設定の合図として控えめに）
- 微調整：`事務局備註` 列幅を 120-220px → 100-200px に縮小
- 修正：シートに `區分=共通` が無いとき「■ 個別Issue（縱軸·依SKU管理）」見出しを抑止
  （部門別の新區分体系では SKU 区分ではないため、古い見出しが誤解を招く）

---

## v0.2.0 — 2026-05-30

**Issue/Task 階層・狀態・事務局備註・更新日・Confluence リンク**

- 追加：オプション列対応 — `種別 / 親編號 / Confluence URL / 狀態 / 事務局備註 / 更新日`
  （シートに無くてもエラーにせず、空欄として描画）
- 追加：Issue 行を親、`種別=Task & 親編號=Issue編號` を子として階層表示
  （デフォルト折りたたみ、編號セルの `▶` クリックで展開）
- 追加：`狀態` を 3 段階バッジで表示（`未開始` / `進行中` / `完成`）
- 追加：`更新日` を「N日前」表示し、>7日 で黄、>14日 で赤に自動色変化
- 追加：`Confluence URL` を 📄 アイコンリンクとして各行に表示
- ドキュメント：
  - `docs/sheet-setup.md` — 事務局向けの Sheet 列追加手順書
  - `docs/confluence-template.md` — 1組1ページ A型テンプレ（State型＋履歴ログ別欄）

---

## v0.1.0 — 2026-05-29 以前

- 既存版：Tab A/B/C/D、Google Sheet からの gviz CSV 取得、管理者専用 Tab E
- CLAUDE.md 初版作成（2026-05-29）
